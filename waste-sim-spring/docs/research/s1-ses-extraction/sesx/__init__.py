"""sesx — 소스 코드에서 SES 초안을 뽑고, 근거·구조를 검사하고, 전문가 검토까지 남기는 도구.

교차검토 판정에 따른 원칙 셋:

1. 결정적 검사는 **승인 산출물 생성**을 막는다. 검토자의 열람을 막지 않는다.
2. 후보는 사라지지 않는다. 병합·분할·보류·거부는 판정 기록(provenance)으로 남는다.
3. 모르는 활성 조건은 `unknown`이다. 무조건 활성으로 바꾸는 경로는 없다.

기존 run_s1.py · run_t.py · runs/ 는 건드리지 않는다. 신규 경로는 exp/<run_id>/ 아래다.
"""

from . import contract  # noqa: F401

__all__ = ["contract"]
