관찰에서 Entity 후보와 필요한 구조 노드를 찾는다. entities와 disputes를 낸다.
stateful의 identity_evidence와 state_evidence를 각각 제시한다. 같은 클래스에 있을 필요는 없다.
명칭만으로 대상을 합치지 않는다. 코드상 식별과 상태 소유가 다른 후보는 구별한다.
boundary/set/type은 상태 변화 개체와 구별하고 구성/반복/선택의 근거를 제시한다.
