# 참조 SES ref-v8 재편 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `reference-ses.json`을 시스템 구성 골격(설정·모델·실험·결과)으로 재편해 ref-v8을 만들고, ref-v7을 별도 파일로 동결해 과거 실행의 채점 기준을 보존한다.

**Architecture:** 정답지는 손으로 고치지 않는다. 불변식 검사기 `check_ref_v8.py`를 먼저 만들어 실패시키고, `reference-ses.json`을 단계별로 고쳐 검사를 통과시킨다. 검사기는 재편이 끝난 뒤에도 남아 정답지의 구조 불변식을 지킨다. 채점기 `score_ses.py`는 이름에 의존하지 않으므로 **한 줄도 고치지 않는다.**

**Tech Stack:** Python 3.14 (표준 라이브러리만), JSON, 기존 `score_ses.py` · `render_ses.py`

**Spec:** `docs/superpowers/specs/2026-09-16-ses-v8-tree-design.md`

## Global Constraints

모든 작업의 요구사항에 아래가 암묵적으로 포함된다.

- **작업 디렉터리는 `waste-sim-spring/docs/research/s1-ses-extraction/`이다.** 이 문서의 모든 상대 경로는 여기를 기준으로 한다.
- **인코딩은 UTF-8이다.** 모든 파일 읽기·쓰기에 `encoding="utf-8"`을 명시한다. Windows 기본 코드페이지로 열면 한글이 깨진다.
- **JSON 쓰기는 `json.dump(..., ensure_ascii=False, indent=1)`이다.** `reference-ses.json`이 현재 indent 1에 한글 그대로다. 다른 값을 쓰면 diff가 전체 파일이 된다.
- **엔티티 51개는 하나도 사라지지 않는다.** 개명 3건과 재배치만 있다. v8의 엔티티는 **62개**다.
- **`couplings` 6건과 `critical_points` 5건의 내용은 불변이다.** 개명에 걸린 이름만 바꾼다.
- **`evidence` 87건의 값은 불변이다.** 개명에 걸린 **키 7개**만 바꾼다.
- **넣지 않는 것:** `엔진 실행` · `DEVS 엔진` · `이벤트 실행 시스템` · `이벤트 집합` 및 그 자식. 그림에 있어도 넣지 않는다 (spec §3).
- **잎 값은 ref-v7을 따른다:** 직업 축 5종, 실험 유형 축 13종, 결과 구성 6종 (spec §4). 그림의 3종·6묶음·5묶음을 따르지 않는다.
- **건드리지 않는 것:** `sesx/` · `extract.py` · `exp/` · `run_s1.py` · `run_t.py` · `runs/` · `tests/` · `score_ses.py` · `build_aliases.py` · `compare.py` · `bench_ollama.py` · `tally_identity.py`.
- **커밋 메시지는 한국어 평서문이다.** 저장소의 기존 형식(`docs(ses): …`)을 따르고, 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>` 을 붙인다.

## 사양서에서 정정된 것 하나

Spec §8-3과 §10은 `score_ses.py`에 `--ref` 인자를 **노출해야 한다**고 적었다. 확인 결과 **이미 있다** (`score_ses.py:288` `ap.add_argument("--ref", default="reference-ses.json")`). 따라서 `score_ses.py`는 이 계획에서 수정 대상이 아니다. Task 1에서 사양서의 그 두 줄을 정정한다.

## File Structure

| 파일 | 상태 | 책임 |
|---|---|---|
| `reference-ses.json` | 수정 | ref-v8 정답지. 최종 산출물 |
| `reference-ses-v7.json` | 신규 | ref-v7 동결본. 과거 실행 12건의 채점 기준 |
| `check_ref_v8.py` | 신규 | 정답지의 구조 불변식 검사기. `skeleton` · `systems` · `settings` · `all` 네 묶음 |
| `aliases.json` | 수정 | 개명 3건 별칭 추가, `SimulationConfig`를 `_skipped`에서 승격 |
| `구성결정-SES-대응표.md` | 수정 | `SES 자리` 열을 v8 경로로 갱신 |
| `참조-검증-기록.md` | 수정 | v7→v8 재편 근거, 새 엔티티 11개의 근거, 미검증 항목 |
| `README.md` | 수정 | 정답지 두 개 병존 규칙 |
| `ses-tree.html` · `ses-mapping.html` · 두 `.png` | 재생성 | `render_ses.py --png` 산출물 |
| `docs/superpowers/specs/2026-09-16-ses-v8-tree-design.md` | 수정 | §8-3·§10의 `--ref` 정정 |

`check_ref_v8.py`가 한 파일인 이유: 검사 대상이 `reference-ses.json` 하나뿐이고, 네 묶음이 같은 JSON을 같은 방식으로 읽는다. 함께 바뀌는 것은 함께 둔다.

---

### Task 1: ref-v7 동결과 사양서 정정

ref-v8을 만들기 전에 되돌아갈 자리를 먼저 확보한다. 이 작업이 끝나면 `reference-ses.json`이 어떻게 망가져도 과거 12건의 채점을 재현할 수 있다.

**Files:**
- Create: `reference-ses-v7.json`
- Modify: `docs/superpowers/specs/2026-09-16-ses-v8-tree-design.md`
- Modify: `README.md`

**Interfaces:**
- Consumes: 없음 (첫 작업)
- Produces: `reference-ses-v7.json` — ref-v7과 **바이트 단위로 동일한** 파일. 이후 모든 작업이 이것을 기준선으로 쓴다.

- [ ] **Step 1: 동결본을 만든다**

```bash
cd waste-sim-spring/docs/research/s1-ses-extraction
cp reference-ses.json reference-ses-v7.json
```

- [ ] **Step 2: 바이트 동일성을 확인한다**

```bash
cmp reference-ses.json reference-ses-v7.json && echo "IDENTICAL"
```

기대: `IDENTICAL` 출력. 아무것도 출력되지 않으면 `cmp`가 차이를 찾은 것이니 다시 복사한다.

- [ ] **Step 3: 동결본으로 과거 실행을 채점해 기준선을 남긴다**

```bash
python score_ses.py --ref reference-ses-v7.json --runs 'runs/gpt-4.1-mini-C*.json' > /tmp/baseline-v7.txt
grep -c "──" /tmp/baseline-v7.txt
```

기대: `12`. 이 글롭이 **과거 12건(C·Cs·Csr·Cd 각 3회)이고 그것만**이다. `runs/*.json` 로 넓히면
`manifest-*.json` 5개(SES 출력이 아니라 실행 목록)와 `DEMO-*.json` 2개(ref-v2로 합성한 파일),
`*-T-*.json` 4개가 섞여 23건이 된다. 기준선 비교가 오염되므로 넓히지 않는다.

이 파일은 커밋하지 않는다 — Task 6·8의 검증에서 비교용으로만 쓴다. 경로를 기억해 둔다.

- [ ] **Step 4: 사양서의 `--ref` 서술을 정정한다**

`docs/superpowers/specs/2026-09-16-ses-v8-tree-design.md` §8 완화 항목 3을 아래로 바꾼다.

```markdown
3. `score_ses.py`의 `--ref` 인자를 쓴다. **이미 있다**(`score_ses.py:288`,
   기본값 `reference-ses.json`). 채점기는 고치지 않는다.
```

같은 문서 §10 표에서 `score_ses.py` 행을 지운다. §11 검증 1·2·3의 명령은 그대로 둔다 — 인자가 이미 있으므로 그대로 동작한다.

- [ ] **Step 5: README에 정답지 병존 규칙을 적는다**

`README.md`의 "## 주의 — DEMO 파일은 ref-v2 기준이다" 절 **앞**에 아래를 넣는다.

```markdown
## 정답지가 둘이다 — v7과 v8

```
reference-ses.json       ref-v8. 시스템 구성 골격(설정·모델·실험·결과). 새 실행은 이것으로 잰다
reference-ses-v7.json    ref-v7 동결본. 과거 실행 12건(C·Cs·Csr·Cd)은 이것으로 잰다
```

v8은 엔티티가 51 → 62개다. 늘어난 11개(설정 6 · 시스템 5)는 추출기가 낼 수 없는 구조이므로,
과거 실행을 v8로 채점하면 재현율이 떨어진다. **그 하락은 추출 품질의 변화가 아니라 정답지
변경의 결과다. 두 수를 같은 표에 놓지 않는다.**

```bash
python score_ses.py --ref reference-ses-v7.json --runs 'runs/gpt-4.1-mini-C*.json'   # 과거 12건
python score_ses.py --runs 'exp/새실행/ses.json'                                     # 새 실행 (기본값이 v8)
```
```

- [ ] **Step 6: 커밋**

```bash
cd /c/Dev/MCP
git add waste-sim-spring/docs/research/s1-ses-extraction/reference-ses-v7.json \
        waste-sim-spring/docs/research/s1-ses-extraction/README.md \
        waste-sim-spring/docs/superpowers/specs/2026-09-16-ses-v8-tree-design.md
git commit -m "$(cat <<'EOF'
docs(ses): ref-v7을 동결하고 정답지 병존 규칙을 적는다

ref-v8은 추출기가 낼 수 없는 구조를 11개 늘린다. 과거 실행 12건을 v8로
채점하면 재현율 하락이 추출 품질이 아니라 정답지 변경의 결과가 된다.
v7을 그대로 복사해 두고 두 수를 섞지 않는다.

score_ses.py의 --ref는 이미 있었다. 사양서의 그 서술을 정정한다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: 불변식 검사기 — 실패하는 상태로 만든다

정답지를 고치기 전에 "무엇이 맞는 것인가"를 실행 가능한 형태로 먼저 적는다. 이 시점에서 검사는 **전부 실패해야 한다.** 실패하지 않으면 검사가 아무것도 재지 않는 것이다.

**Files:**
- Create: `check_ref_v8.py`

**Interfaces:**
- Consumes: `reference-ses.json` (아직 v7 내용)
- Produces: CLI `python check_ref_v8.py {skeleton|systems|settings|all}` — 통과 시 종료코드 0, 실패 시 1과 실패 목록. Task 3·4·5가 각각 `skeleton` · `systems` · `settings`를 통과시킨다.

- [ ] **Step 1: 검사기를 쓴다**

`check_ref_v8.py` 전문:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
check_ref_v8.py — 참조 SES(ref-v8)의 구조 불변식 검사

사용:
  python check_ref_v8.py skeleton    # 최상위 골격·개명·보존량
  python check_ref_v8.py systems     # DEVS 모델 구성 5종과 그 아래
  python check_ref_v8.py settings    # 설정 구성 5종과 sets 대응
  python check_ref_v8.py all         # 위 셋 + 전역 불변식

채점기(score_ses.py)와 역할이 다르다. 채점기는 추출 결과를 정답지와 견주고,
이것은 정답지 자체가 스스로 모순되지 않는지 본다.
"""
from __future__ import annotations
import io, json, os, sys

KIT = os.path.dirname(os.path.abspath(__file__))
REF = os.path.join(KIT, "reference-ses.json")

# ---------------------------------------------------------------- 기대값
ROOT = "장량동 생활쓰레기 수거 시뮬레이터"

TOP = ["시뮬레이션 설정", "생활쓰레기 수거 모델", "시나리오 실험", "시뮬레이션 결과"]

SYSTEMS = {
    "거주민 배출 시스템": ["거주민 집합"],
    "수거장 적재 시스템": ["수거지점 집합", "폐기물 유형 집합"],
    "차량 수거 시스템": ["수거차량 집합"],
    "경로 교통 시스템": ["수거 경로", "교통구역 집합"],
    "민원 감시 시스템": ["민원 판정"],
}

SETTINGS = ["거주민·배출 설정", "수거지점·폐기물 설정", "차량·수거 설정",
            "교통·경로 설정", "실험 설정"]

RENAMED = {"대상 시스템": "생활쓰레기 수거 모델",
           "실험": "시나리오 실험",
           "관측": "시뮬레이션 결과"}

BANNED = ["엔진 실행", "DEVS 엔진", "이벤트 실행 시스템", "이벤트 집합"]

N_ENTITIES = 62
N_COUPLINGS = 6
N_CRITICAL = 5
N_EVIDENCE = 87

# 잎 값 — ref-v7을 따른다
LEAF_COUNTS = {
    ("거주민", "직업 축"): 5,
    ("거주민", "배출시각 모델 축"): 2,
    ("수거차량", "차종 축"): 3,
    ("수거 경로", "이동시간 방식 축"): 3,
    ("민원 판정", "판정 구성"): 3,
    ("시나리오 실험", "실험 유형 축"): 13,
    ("시뮬레이션 결과", "결과 구성"): 6,
}

# 구성결정-SES-대응표의 29개 결정. 문항 16·17이 한 속성으로 합쳐져 항목은 28개다.
# (설정 엔티티, 속성) -> target
SETS_EXPECTED = {
    "거주민·배출 설정": {
        "거주민수": "거주민 집합.개수",
        "직업구성": "거주민 집합.직업구성",
        "1인배출량": "생활쓰레기 수거 모델.1인배출량",
        "배출량변동": "생활쓰레기 수거 모델.배출량변동",
        "외출시각변동": "거주민.외출시각변동",
        "배출시각모델": "거주민.배출시각 모델 축",
        "배출허용창": "포항시 배출시간대 기반.배출허용창",
    },
    "수거지점·폐기물 설정": {
        "수거지점수": "수거지점 집합.개수",
        "용량": "폐기물 유형.용량",
        "임계값": "폐기물 유형.임계값",
    },
    "차량·수거 설정": {
        "수거시각": "수거차량.수거시각",
        "수거요일": "수거차량.수거요일",
        "차종": "수거차량.차종 축",
        "차량수": "수거차량 집합.개수",
        "적재용량": "수거차량.적재용량",
        "초기적재량": "수거차량.초기적재량",
        "배차간격": "수거차량.배차간격",
        "지점당수거시간": "수거차량.지점당수거시간",
    },
    "교통·경로 설정": {
        "교통모드": "결합: 교통 구역.혼잡계수 -> 수거 경로.이동시간 · 교통혼잡 판정.판정 의 active_when",
        "시간대프로파일": "교통 구역.시간대프로파일",
        "이동시간방식": "수거 경로.이동시간 방식 축",
        "구간이동시간": "구간 상수.구간이동시간",
        "구역내이동시간": "교통구역 근사.구역내이동시간",
        "구역배정가정": "교통구역 근사.구역배정가정",
        "방문순서": "수거 경로.방문순서",
    },
    "실험 설정": {
        "실험유형": "시나리오 실험.실험 유형 축",
        "기간": "시나리오 실험.기간",
        "반복횟수": "시나리오 실험.반복횟수",
    },
}

# ---------------------------------------------------------------- 도우미
def load():
    return json.load(io.open(REF, encoding="utf-8"))

def dec_of(ents, name, dec_name):
    """엔티티의 특정 이름 분해를 돌려준다. 없으면 None."""
    for d in ents.get(name, {}).get("decompositions", []) or []:
        if d.get("name") == dec_name:
            return d
    return None

def children_of(ents, name):
    out = []
    for d in ents.get(name, {}).get("decompositions", []) or []:
        out += list(d.get("children") or [])
    return out

def all_edges(ents):
    for p, body in ents.items():
        for d in body.get("decompositions", []) or []:
            for c in d.get("children") or []:
                yield p, c

# ---------------------------------------------------------------- 검사
def check_skeleton(ref, bad):
    ents = ref["entities"]

    if ref.get("root") != ROOT:
        bad.append(f"root가 {ref.get('root')!r} 이다. {ROOT!r} 이어야 한다")

    d = dec_of(ents, ROOT, "시스템 구성")
    if d is None:
        bad.append("루트에 aspect「시스템 구성」 분해가 없다")
    else:
        if d.get("kind") != "aspect":
            bad.append(f"「시스템 구성」의 kind가 {d.get('kind')!r} 이다. aspect 이어야 한다")
        if list(d.get("children") or []) != TOP:
            bad.append(f"「시스템 구성」의 자식이 {d.get('children')} 이다. {TOP} 이어야 한다")

    if dec_of(ents, ROOT, "최상위") is not None:
        bad.append("루트에 옛 분해「최상위」가 남아 있다")

    for old, new in RENAMED.items():
        if old in ents:
            bad.append(f"옛 이름 {old!r} 이 엔티티로 남아 있다 ({new!r} 으로 개명해야 한다)")
        if new not in ents:
            bad.append(f"새 이름 {new!r} 엔티티가 없다")

    for b in BANNED:
        if b in ents:
            bad.append(f"넣지 않기로 한 {b!r} 이 엔티티로 있다 (사양서 3절)")

    for (owner, axis), n in LEAF_COUNTS.items():
        dd = dec_of(ents, owner, axis)
        if dd is None:
            bad.append(f"{owner}에 분해「{axis}」가 없다")
        elif len(dd.get("children") or []) != n:
            bad.append(f"{owner}「{axis}」의 자식이 {len(dd.get('children') or [])}개다. {n}개여야 한다")

    for label, got, want in (("엔티티", len(ents), N_ENTITIES),
                             ("결합", len(ref.get("couplings") or []), N_COUPLINGS),
                             ("임계점", len(ref.get("critical_points") or []), N_CRITICAL),
                             ("근거", len(ref.get("evidence") or {}), N_EVIDENCE)):
        if got != want:
            bad.append(f"{label} 수가 {got}이다. {want}이어야 한다")

    for old in RENAMED:
        for k in ref.get("evidence") or {}:
            if k == old or k.startswith(old + "."):
                bad.append(f"근거 키 {k!r} 가 옛 이름을 쓴다")
        for c in ref.get("couplings") or []:
            for side in ("from", "to"):
                if str(c.get(side, "")).split(".")[0] == old:
                    bad.append(f"결합의 {side} {c.get(side)!r} 가 옛 이름을 쓴다")
        for cp in ref.get("critical_points") or []:
            if str(cp.get("where", "")).split(".")[0] == old:
                bad.append(f"임계점 {cp.get('id')} 의 where 가 옛 이름을 쓴다")

def check_systems(ref, bad):
    ents = ref["entities"]

    d = dec_of(ents, "생활쓰레기 수거 모델", "DEVS 모델 구성")
    if d is None:
        bad.append("「생활쓰레기 수거 모델」에 aspect「DEVS 모델 구성」이 없다")
    else:
        if d.get("kind") != "aspect":
            bad.append(f"「DEVS 모델 구성」의 kind가 {d.get('kind')!r} 이다. aspect 이어야 한다")
        want = list(SYSTEMS)
        if list(d.get("children") or []) != want:
            bad.append(f"「DEVS 모델 구성」의 자식이 {d.get('children')} 이다. {want} 이어야 한다")

    if dec_of(ents, "생활쓰레기 수거 모델", "구성") is not None:
        bad.append("「생활쓰레기 수거 모델」에 옛 분해「구성」이 남아 있다")

    for sysname, kids in SYSTEMS.items():
        if sysname not in ents:
            bad.append(f"시스템 {sysname!r} 엔티티가 없다")
            continue
        got = children_of(ents, sysname)
        if got != kids:
            bad.append(f"{sysname}의 자식이 {got} 이다. {kids} 이어야 한다")
        if sysname not in (ref.get("evidence") or {}):
            bad.append(f"{sysname}에 근거가 없다")

def check_settings(ref, bad):
    ents = ref["entities"]

    d = dec_of(ents, "시뮬레이션 설정", "설정 구성")
    if d is None:
        bad.append("「시뮬레이션 설정」에 aspect「설정 구성」이 없다")
    else:
        if d.get("kind") != "aspect":
            bad.append(f"「설정 구성」의 kind가 {d.get('kind')!r} 이다. aspect 이어야 한다")
        if list(d.get("children") or []) != SETTINGS:
            bad.append(f"「설정 구성」의 자식이 {d.get('children')} 이다. {SETTINGS} 이어야 한다")

    if "시뮬레이션 설정" in ents and "시뮬레이션 설정" not in (ref.get("evidence") or {}):
        bad.append("「시뮬레이션 설정」에 근거가 없다")

    if "sets" not in (ref.get("_meta", {}).get("schema_extensions") or {}):
        bad.append("_meta.schema_extensions 에 sets 설명이 없다")

    n_sets = 0
    for owner, expected in SETS_EXPECTED.items():
        if owner not in ents:
            bad.append(f"설정 {owner!r} 엔티티가 없다")
            continue
        if owner not in (ref.get("evidence") or {}):
            bad.append(f"{owner}에 근거가 없다")
        if children_of(ents, owner):
            bad.append(f"{owner}는 자식을 갖지 않아야 한다 (모델 엔티티를 다시 소유하지 않는다)")
        sets = ents[owner].get("sets") or {}
        attrs = ents[owner].get("attrs") or []
        for attr, target in expected.items():
            n_sets += 1
            if attr not in attrs:
                bad.append(f"{owner}.attrs 에 {attr!r} 가 없다")
            if attr not in sets:
                bad.append(f"{owner}.sets 에 {attr!r} 가 없다")
                continue
            got = sets[attr].get("target")
            if got != target:
                bad.append(f"{owner}.sets[{attr!r}].target 가 {got!r} 이다. {target!r} 이어야 한다")
            if not str(sets[attr].get("evidence") or "").strip():
                bad.append(f"{owner}.sets[{attr!r}] 에 evidence 가 비어 있다")
        for attr in sets:
            if attr not in expected:
                bad.append(f"{owner}.sets 에 대응표에 없는 {attr!r} 가 있다")

    if n_sets != 28:
        bad.append(f"sets 항목이 {n_sets}개다. 28개여야 한다 "
                   f"(대응표의 결정 29개 중 문항 16·17이 수거시각 하나로 합쳐진다)")

    # target 이 실재하는 자리를 가리키는가
    for owner, sets in ((o, ents.get(o, {}).get("sets") or {}) for o in SETS_EXPECTED):
        for attr, body in sets.items():
            t = str(body.get("target") or "")
            if t.startswith("결합:"):
                continue                      # 문항 24 한 건만 이 형식이다
            ent, _, tail = t.partition(".")
            if ent not in ents:
                bad.append(f"{owner}.sets[{attr!r}] 가 없는 엔티티 {ent!r} 를 가리킨다")
                continue
            is_attr = tail in (ents[ent].get("attrs") or [])
            is_axis = dec_of(ents, ent, tail) is not None
            if not (is_attr or is_axis):
                bad.append(f"{owner}.sets[{attr!r}] 의 {tail!r} 가 {ent!r} 의 속성도 분해도 아니다")

def check_global(ref, bad):
    ents = ref["entities"]

    parents = {}
    for p, c in all_edges(ents):
        if c not in ents:
            bad.append(f"{p} 의 자식 {c!r} 가 엔티티에 없다")
            continue
        parents.setdefault(c, []).append(p)

    for c, ps in parents.items():
        if len(ps) > 1:
            bad.append(f"{c!r} 의 부모가 {len(ps)}개다: {ps}")

    reach, stack = {ROOT}, [ROOT]
    while stack:
        n = stack.pop()
        for c in children_of(ents, n):
            if c in ents and c not in reach:
                reach.add(c); stack.append(c)
    for n in ents:
        if n not in reach:
            bad.append(f"{n!r} 이 루트에서 닿지 않는다 (고아)")

    for n in ents:
        if n in parents and n == ROOT:
            bad.append("루트에 부모가 있다 (순환)")

    for n in ents:
        if n not in (ref.get("evidence") or {}):
            has_attr_ev = any(k.startswith(n + ".") for k in (ref.get("evidence") or {}))
            if not has_attr_ev:
                bad.append(f"{n!r} 에 근거가 없다")

GROUPS = {"skeleton": [check_skeleton],
          "systems": [check_systems],
          "settings": [check_settings],
          "all": [check_skeleton, check_systems, check_settings, check_global]}

if __name__ == "__main__":
    group = sys.argv[1] if len(sys.argv) > 1 else "all"
    if group not in GROUPS:
        print(f"묶음은 {list(GROUPS)} 중 하나입니다"); sys.exit(2)
    ref = load()
    bad = []
    for fn in GROUPS[group]:
        fn(ref, bad)
    if bad:
        print(f"[{group}] 불변식 위반 {len(bad)}건")
        for b in bad:
            print(f"   X {b}")
        sys.exit(1)
    print(f"[{group}] 통과")
```

- [ ] **Step 2: 세 묶음이 모두 실패하는지 확인한다**

```bash
python check_ref_v8.py skeleton; echo "exit=$?"
python check_ref_v8.py systems;  echo "exit=$?"
python check_ref_v8.py settings; echo "exit=$?"
```

기대: 셋 다 `exit=1`. `skeleton`은 "루트에 aspect「시스템 구성」 분해가 없다" · 개명 3건 · 엔티티 수 51 등을 보고해야 한다. `systems`는 "「생활쓰레기 수거 모델」에 aspect「DEVS 모델 구성」이 없다"부터, `settings`는 "「시뮬레이션 설정」에 aspect「설정 구성」이 없다"부터 보고해야 한다.

어느 하나라도 `exit=0`이면 그 검사는 아무것도 재지 않는 것이다. 멈추고 원인을 찾는다.

- [ ] **Step 3: 커밋**

```bash
cd /c/Dev/MCP
git add waste-sim-spring/docs/research/s1-ses-extraction/check_ref_v8.py
git commit -m "$(cat <<'EOF'
test(ses): 정답지의 구조 불변식을 실행 가능한 검사로 적는다

정답지를 손으로 고치면 어긋난 것을 눈으로 찾아야 한다. 무엇이 맞는
것인가를 먼저 적고, 정답지를 고쳐 통과시킨다. 지금은 셋 다 실패한다.

채점기와 역할이 다르다 — 채점기는 추출 결과를 정답지와 견주고,
이것은 정답지가 스스로 모순되지 않는지 본다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: 골격 — 개명 3건과 최상위 재편

`skeleton` 검사를 통과시킨다. 이 작업이 끝나면 엔티티가 62개가 되고 최상위가 4형제가 된다. 새 엔티티 11개는 **껍데기로 먼저 넣고**, 내용은 Task 4·5가 채운다.

**Files:**
- Modify: `reference-ses.json`

**Interfaces:**
- Consumes: Task 2의 `check_ref_v8.py skeleton`
- Produces: `reference-ses.json` — 엔티티 62개, 루트 `aspect「시스템 구성」` 4자식, 개명 3건 반영. 새 엔티티 11개는 `{"attrs": [], "decompositions": []}` 상태이고 `evidence`에 아직 없다.

- [ ] **Step 1: 검사가 실패하는 것을 확인한다**

```bash
python check_ref_v8.py skeleton; echo "exit=$?"
```

기대: `exit=1`.

- [ ] **Step 2: 개명과 골격 재편을 적용한다**

아래를 `/tmp/step3.py` 로 저장하고 작업 디렉터리에서 실행한다. 손으로 JSON을 고치지 않는다 — 87개 근거 키와 6개 결합을 빠짐없이 옮기는 일은 눈으로 하면 반드시 빠진다.

```python
# -*- coding: utf-8 -*-
import io, json, collections

REF = "reference-ses.json"
RENAMED = {"대상 시스템": "생활쓰레기 수거 모델",
           "실험": "시나리오 실험",
           "관측": "시뮬레이션 결과"}

ref = json.load(io.open(REF, encoding="utf-8"),
                object_pairs_hook=collections.OrderedDict)

def rn(name):
    return RENAMED.get(name, name)

def rn_dotted(s):
    head, dot, tail = str(s).partition(".")
    return rn(head) + dot + tail

# 1) 엔티티 개명 — 삽입 순서를 지킨다
ents = collections.OrderedDict((rn(k), v) for k, v in ref["entities"].items())
for body in ents.values():
    for d in body.get("decompositions") or []:
        d["children"] = [rn(c) for c in (d.get("children") or [])]

# 2) 루트 분해 교체
TOP = ["시뮬레이션 설정", "생활쓰레기 수거 모델", "시나리오 실험", "시뮬레이션 결과"]
ents[ref["root"]]["decompositions"] = [
    {"kind": "aspect", "name": "시스템 구성", "children": TOP}]

# 3) 「관측 구성」 -> 「결과 구성」
for d in ents["시뮬레이션 결과"]["decompositions"]:
    if d.get("name") == "관측 구성":
        d["name"] = "결과 구성"

# 4) 새 엔티티 11개를 껍데기로 넣는다
NEW = ["시뮬레이션 설정",
       "거주민·배출 설정", "수거지점·폐기물 설정", "차량·수거 설정",
       "교통·경로 설정", "실험 설정",
       "거주민 배출 시스템", "수거장 적재 시스템", "차량 수거 시스템",
       "경로 교통 시스템", "민원 감시 시스템"]
for n in NEW:
    assert n not in ents, n
    ents[n] = collections.OrderedDict([("attrs", []), ("decompositions", [])])

ref["entities"] = ents

# 5) 근거 키 개명 (값은 건드리지 않는다)
ref["evidence"] = collections.OrderedDict(
    (rn_dotted(k), v) for k, v in ref["evidence"].items())

# 6) 결합·임계점의 이름 개명 (내용은 건드리지 않는다)
for c in ref["couplings"]:
    c["from"], c["to"] = rn_dotted(c["from"]), rn_dotted(c["to"])
for cp in ref["critical_points"]:
    cp["where"] = rn_dotted(cp["where"])

# 7) _meta
ref["_meta"]["version"] = "ref-v8"
ref["_meta"]["verified_against"] = "develop @ 12937be · src/main/java/com/wastesim/**"
ref["_meta"]["corrections"].append(
    "ref-v8 재편 — 최상위를 시스템 구성 골격으로 바꿨다. 대상 시스템→생활쓰레기 수거 모델 · "
    "실험→시나리오 실험 · 관측→시뮬레이션 결과. 엔진 실행·이벤트 실행 시스템은 넣지 않는다 "
    "— ref-v7이 실행 수단으로 제외한 판단을 유지한다(_meta.note)")

json.dump(ref, io.open(REF, "w", encoding="utf-8", newline="\n"),
          ensure_ascii=False, indent=1)
print("엔티티", len(ref["entities"]), "· 근거", len(ref["evidence"]))
```

```bash
python /tmp/step3.py
```

기대 출력: `엔티티 62 · 근거 87`

- [ ] **Step 3: 검사가 통과하는지 확인한다**

```bash
python check_ref_v8.py skeleton; echo "exit=$?"
```

기대: `[skeleton] 통과` · `exit=0`.

`systems` · `settings` 는 아직 실패해야 한다. 확인한다:

```bash
python check_ref_v8.py systems; echo "exit=$?"
python check_ref_v8.py settings; echo "exit=$?"
```

기대: 둘 다 `exit=1`.

- [ ] **Step 4: 채점기가 살아 있는지 확인한다**

```bash
python score_ses.py --selftest
```

기대: `자체 검증 6/6 통과`. 이 검증은 `민원 판정` · `거주민` · `폐기물 유형` · `수거지점`을 이름으로 쓰는데 넷 다 v8에 그대로 있다. 실패하면 개명이 의도보다 넓게 번진 것이다.

- [ ] **Step 5: 커밋**

```bash
cd /c/Dev/MCP
git add waste-sim-spring/docs/research/s1-ses-extraction/reference-ses.json
git commit -m "$(cat <<'EOF'
feat(ses): 정답지의 최상위를 시스템 구성 골격으로 바꾼다

대상 시스템·실험·관측 세 형제는 시뮬레이터를 구성하는 절차를 보여 주지
않는다. 설정·모델·실험·결과를 1급 가지로 올리고 셋을 개명한다.

새 엔티티 11개는 껍데기로만 넣는다. 내용은 뒤따르는 작업이 채운다.
결합 6건·임계점 5건·근거 87건의 값은 건드리지 않았고, 이름만 옮겼다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: DEVS 모델 구성 — 다섯 시스템

`systems` 검사를 통과시킨다. `생활쓰레기 수거 모델`의 자식 7개가 5개 시스템 아래로 한 계층 내려간다.

**Files:**
- Modify: `reference-ses.json`

**Interfaces:**
- Consumes: Task 3의 골격 (엔티티 62개, 껍데기 11개)
- Produces: 다섯 시스템이 각자 자식과 근거를 갖는다. `생활쓰레기 수거 모델`의 분해가 `aspect「DEVS 모델 구성」` 하나로 바뀐다.

- [ ] **Step 1: 검사가 실패하는 것을 확인한다**

```bash
python check_ref_v8.py systems; echo "exit=$?"
```

기대: `exit=1`.

- [ ] **Step 2: 다섯 시스템을 채운다**

`/tmp/step4.py`:

```python
# -*- coding: utf-8 -*-
import io, json, collections

REF = "reference-ses.json"
ref = json.load(io.open(REF, encoding="utf-8"),
                object_pairs_hook=collections.OrderedDict)
ents = ref["entities"]

SYSTEMS = collections.OrderedDict([
    ("거주민 배출 시스템", ("배출 구성", ["거주민 집합"])),
    ("수거장 적재 시스템", ("적재 구성", ["수거지점 집합", "폐기물 유형 집합"])),
    ("차량 수거 시스템", ("수거 구성", ["수거차량 집합"])),
    ("경로 교통 시스템", ("경로 구성", ["수거 경로", "교통구역 집합"])),
    ("민원 감시 시스템", ("감시 구성", ["민원 판정"])),
])

EVIDENCE = {
 "거주민 배출 시스템":
   "SimulationEngine:363 거주민별 배출 이벤트 사전 생성 · DischargeEvt(227) 처리(:457). "
   "거주민 집합이 배출을 만들어 내는 한 덩어리다 — 묶음의 근거이고 새 개체의 주장이 아니다",
 "수거장 적재 시스템":
   "SimulationEngine:457 fill[building][type] += amount * WasteType.fraction · "
   "peak 갱신. 적재 상태를 갖는 것은 수거지점과 폐기물 유형 둘뿐이다",
 "차량 수거 시스템":
   "SimulationEngine CollectEvt(213) 처리(:416-425) · TruckType 용량과 기동성. "
   "수거차량 집합이 수거를 수행하는 한 덩어리다",
 "경로 교통 시스템":
   "RoutePlanner.assignRoutes(cfg) 로 경로를 짜고 TravelTimeCalculator·TrafficProfile 이 "
   "이동시간을 정한다(SimulationEngine:336,:343). 경로와 교통 구역은 함께 움직인다",
 "민원 감시 시스템":
   "SimulationEngine:460 wasteOverflowComplaints++ · :473 landlordComplaints++ · "
   ":483 total. InspectEvt(236)가 별도 시각에 도착한다. 판정 셋이 한 덩어리다",
}

for name, (dec_name, kids) in SYSTEMS.items():
    for k in kids:
        assert k in ents, k
    ents[name]["decompositions"] = [
        {"kind": "aspect", "name": dec_name, "children": kids}]
    ref["evidence"][name] = EVIDENCE[name]

ents["생활쓰레기 수거 모델"]["decompositions"] = [
    {"kind": "aspect", "name": "DEVS 모델 구성", "children": list(SYSTEMS)}]

ref["_meta"]["corrections"].append(
    "대상 시스템의 자식 7개를 DEVS 모델 구성 5종 아래로 한 계층 내렸다. 자식 자체는 바뀌지 "
    "않는다. 다섯 시스템의 근거는 묶음의 근거다 — 새 개체를 주장하는 것이 아니라, 이미 근거가 "
    "있는 개체들이 코드에서 한 덩어리로 돈다는 사실을 가리킨다")

json.dump(ref, io.open(REF, "w", encoding="utf-8", newline="\n"),
          ensure_ascii=False, indent=1)
print("근거", len(ref["evidence"]))
```

```bash
python /tmp/step4.py
```

기대 출력: `근거 92` (87 + 시스템 5건)

- [ ] **Step 3: 검사가 통과하는지 확인한다**

```bash
python check_ref_v8.py systems; echo "exit=$?"
```

기대: `[systems] 통과` · `exit=0`.

- [ ] **Step 4: `skeleton`이 근거 수 때문에 깨진 것을 고친다**

```bash
python check_ref_v8.py skeleton; echo "exit=$?"
```

기대: `exit=1`, "근거 수가 92이다. 87이어야 한다" 한 건만. 다른 위반이 함께 나오면 Step 2가 의도보다 넓게 바꾼 것이니 멈춘다.

`check_ref_v8.py`의 `N_EVIDENCE` 를 고친다.

```python
N_EVIDENCE = 92          # 87(v7) + 시스템 5. 설정 6은 Task 5에서 98이 된다
```

```bash
python check_ref_v8.py skeleton; echo "exit=$?"
```

기대: `[skeleton] 통과` · `exit=0`.

- [ ] **Step 5: 채점기 자체 검증**

```bash
python score_ses.py --selftest
```

기대: `자체 검증 6/6 통과`.

- [ ] **Step 6: 커밋**

```bash
cd /c/Dev/MCP
git add waste-sim-spring/docs/research/s1-ses-extraction/reference-ses.json \
        waste-sim-spring/docs/research/s1-ses-extraction/check_ref_v8.py
git commit -m "$(cat <<'EOF'
feat(ses): 모델 가지를 DEVS 모델 구성 다섯으로 묶는다

대상 시스템 아래 일곱이 나란히 있던 것을 배출·적재·수거·경로·감시
다섯 덩어리로 묶는다. 자식 자체는 바뀌지 않는다.

다섯의 근거는 묶음의 근거다. 새 개체를 주장하지 않고, 이미 근거가 있는
개체들이 코드에서 한 덩어리로 돈다는 사실을 가리킨다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: 설정 가지와 `sets`

`settings` 검사를 통과시킨다. `구성결정-SES-대응표.md`의 29개 결정이 트리로 올라온다.

**Files:**
- Modify: `reference-ses.json`

**Interfaces:**
- Consumes: Task 3의 골격, Task 4의 시스템
- Produces: `시뮬레이션 설정` 아래 5개 설정 엔티티. 각각 `attrs`와 `sets`를 갖고, `sets` 항목 총 28개(결정 29개 중 문항 16·17이 `수거시각` 하나로 합쳐진다). `_meta.schema_extensions.sets` 추가.

- [ ] **Step 1: 검사가 실패하는 것을 확인한다**

```bash
python check_ref_v8.py settings; echo "exit=$?"
```

기대: `exit=1`.

- [ ] **Step 2: 설정 가지를 채운다**

`/tmp/step5.py`:

```python
# -*- coding: utf-8 -*-
import io, json, collections

REF = "reference-ses.json"
ref = json.load(io.open(REF, encoding="utf-8"),
                object_pairs_hook=collections.OrderedDict)
ents = ref["entities"]

TRAFFIC_TARGET = ("결합: 교통 구역.혼잡계수 -> 수거 경로.이동시간 · "
                  "교통혼잡 판정.판정 의 active_when")

SETS = collections.OrderedDict([
 ("거주민·배출 설정", collections.OrderedDict([
   ("거주민수",     ("거주민 집합.개수", "문항 5 residentsPerBuilding -> setResidentsPerBuilding(JangnyangScenarioBuilder:235). multi-aspect의 건물당 복제 수")),
   ("직업구성",     ("거주민 집합.직업구성", "문항 6 occupationPreset -> 직업 배정 목록(:241). 직업 축 자식들에 대한 분포다")),
   ("1인배출량",    ("생활쓰레기 수거 모델.1인배출량", "문항 9 wasteMeanKg -> SimulationConfig.wasteMeanKg:26. 거주민별이 아니라 전역값이다")),
   ("배출량변동",   ("생활쓰레기 수거 모델.배출량변동", "문항 10 wasteSigma -> setWasteSigma. 배출량이 전역이므로 그 변동도 전역이다")),
   ("외출시각변동", ("거주민.외출시각변동", "문항 11 leaveSigma -> sampleOffset")),
   ("배출시각모델", ("거주민.배출시각 모델 축", "문항 12 dischargeTimeMode -> dischargeOffset 분기. spec 축에서 하나를 고른다")),
   ("배출허용창",   ("포항시 배출시간대 기반.배출허용창", "문항 13 dischargeWindow -> getDischargeWindowStartMinutes + span. 코드는 창 안에서 균등 추출한다")),
 ])),
 ("수거지점·폐기물 설정", collections.OrderedDict([
   ("수거지점수", ("수거지점 집합.개수", "문항 4 numBuildings -> setNumBuildings(:204). multi-aspect의 복제 수")),
   ("용량",       ("폐기물 유형.용량", "문항 14 capacity -> WasteType.single(capacity, ...)(:205). 소유자는 지점이 아니라 유형이다 — CP-2")),
   ("임계값",     ("폐기물 유형.임계값", "문항 15 threshold -> WasteType.threshold:22, 기본값은 전역 cfg.threshold:28. 분리배출을 켜면 유형별로 달라진다 — CP-2")),
 ])),
 ("차량·수거 설정", collections.OrderedDict([
   ("수거시각",       ("수거차량.수거시각", "문항 16 collectionTime -> setCollectionTimeMinutes · 문항 17 collectionTimes -> 하루 여러 슬롯. 같은 결정의 목록형이다")),
   ("수거요일",       ("수거차량.수거요일", "문항 18 collectionSchedule -> setCollectionIntervalDays / applyDaysOfWeek 분기")),
   ("차종",           ("수거차량.차종 축", "문항 19 truckType -> setTruckType. spec 축에서 하나를 고른다")),
   ("차량수",         ("수거차량 집합.개수", "문항 20 truckCount -> setNumTrucks(:212)")),
   ("적재용량",       ("수거차량.적재용량", "문항 21 routeAvailableCapacityKg -> setRouteAvailableCapacityKg")),
   ("초기적재량",     ("수거차량.초기적재량", "문항 22 initialTruckLoadKg -> setInitialTruckLoadKg")),
   ("배차간격",       ("수거차량.배차간격", "문항 23 dispatchIntervalMinutes -> setDispatchIntervalMinutes")),
   ("지점당수거시간", ("수거차량.지점당수거시간", "문항 28 serviceMinutesPerSite -> 모든 지점에 더하는 정차시간")),
 ])),
 ("교통·경로 설정", collections.OrderedDict([
   ("교통모드",       (TRAFFIC_TARGET, "문항 24 trafficMode -> setTrafficEnabled(:338). NONE이면 trafficProfile이 null이라 결합 둘이 통째로 죽는다(SimulationEngine:271 · 330-339). 속성이 아니라 결합의 활성 조건을 정하는 유일한 결정이다")),
   ("시간대프로파일", ("교통 구역.시간대프로파일", "문항 25 trafficProfileId -> setTrafficProfileId")),
   ("이동시간방식",   ("수거 경로.이동시간 방식 축", "문항 26 travelTimeMode -> TravelTimeCalculator 분기. CP-4가 이것으로 확정된다")),
   ("구간이동시간",   ("구간 상수.구간이동시간", "문항 27 routeTravelMinutes -> hopMinutes의 상수 항")),
   ("구역내이동시간", ("교통구역 근사.구역내이동시간", "문항 29 intraZoneTravelMinutes -> 같은 구역 안 이동")),
   ("구역배정가정",   ("교통구역 근사.구역배정가정", "문항 30 zoneAssignmentRule -> 건물에서 구역으로의 배정 가정")),
   ("방문순서",       ("수거 경로.방문순서", "문항 31 routeSequence -> RoutePlanner 방문 순서")),
 ])),
 ("실험 설정", collections.OrderedDict([
   ("실험유형",   ("시나리오 실험.실험 유형 축", "문항 2 scenarioType -> spec.scenarioType() 실행 유형 분기")),
   ("기간",       ("시나리오 실험.기간", "문항 7 days -> setDays(:202)")),
   ("반복횟수",   ("시나리오 실험.반복횟수", "문항 8 seeds -> setSeeds")),
 ])),
])

SET_EVIDENCE = {
 "시뮬레이션 설정":
   "model/SimulationConfig.java:10 — 문항 답변이 모이는 자리. "
   "JangnyangScenarioBuilder.toConfig():193 이 답변을 이 객체로 옮긴다",
 "거주민·배출 설정": "JangnyangScenarioBuilder.toConfig() 거주민·배출 구간(:227-241)",
 "수거지점·폐기물 설정": "JangnyangScenarioBuilder.toConfig() 지점·유형 구간(:204-205)",
 "차량·수거 설정": "JangnyangScenarioBuilder.toConfig() 차량 구간(:212 외)",
 "교통·경로 설정": "JangnyangScenarioBuilder.toConfig() 교통·경로 구간(:338 외)",
 "실험 설정": "JangnyangScenarioBuilder.toConfig() 실험 구간(:202 외)",
}

n = 0
for owner, items in SETS.items():
    assert owner in ents, owner
    ents[owner]["attrs"] = list(items)
    ents[owner]["decompositions"] = []
    ents[owner]["sets"] = collections.OrderedDict(
        (attr, collections.OrderedDict([("target", t), ("evidence", ev)]))
        for attr, (t, ev) in items.items())
    ref["evidence"][owner] = SET_EVIDENCE[owner]
    n += len(items)

ents["시뮬레이션 설정"]["decompositions"] = [
    {"kind": "aspect", "name": "설정 구성", "children": list(SETS)}]
ref["evidence"]["시뮬레이션 설정"] = SET_EVIDENCE["시뮬레이션 설정"]

ref["_meta"]["schema_extensions"]["sets"] = (
    "설정의 한 결정이 모델 트리의 어느 자리를 정하는가. target은 '엔티티.속성' 또는 "
    "'엔티티.축이름' 이고, 후자는 그 결정이 spec 축에서 하나를 고른다는 뜻이다. 문항 24 "
    "교통모드 한 건만 결합의 active_when을 가리켜 형식이 다르다. active_when·attr_spec과 "
    "같이 추출 프롬프트에 넣지 않으며 score_ses.py도 채점하지 않는다")

ref["_meta"]["corrections"].append(
    "시뮬레이션 설정 가지를 세우고 구성결정-SES-대응표의 29개를 sets로 이었다. 설정 엔티티는 "
    "모델 엔티티를 다시 소유하지 않는다 — 부모가 둘이 되기 때문이다. 축을 고르는 결정은 "
    "선택자 속성 하나로만 적는다. 넣지 않은 5개(simulationGoal·engine·승인 3종)는 그대로 둔다")

json.dump(ref, io.open(REF, "w", encoding="utf-8", newline="\n"),
          ensure_ascii=False, indent=1)
print("sets", n, "· 근거", len(ref["evidence"]), "· 엔티티", len(ref["entities"]))
```

```bash
python /tmp/step5.py
```

기대 출력: `sets 28 · 근거 98 · 엔티티 62`

28이 아니면 `SETS` 사전의 항목이 빠지거나 겹친 것이다. 29가 아닌 이유는 문항 16 `collectionTime`과
문항 17 `collectionTimes`가 같은 결정의 목록형이라 속성 `수거시각` 하나를 공유하기 때문이다.

- [ ] **Step 3: `N_EVIDENCE`를 98로 고치고 전부 통과시킨다**

`check_ref_v8.py`:

```python
N_EVIDENCE = 98          # 87(v7) + 시스템 5 + 설정 6
```

```bash
python check_ref_v8.py all; echo "exit=$?"
```

기대: `[all] 통과` · `exit=0`. 이 시점에 골격·시스템·설정·전역 불변식이 모두 선다 — 부모가 둘인 엔티티 0건, 고아 0건, `sets.target` 28개가 모두 실재하는 자리를 가리킨다.

실패하면 위반 목록이 원인을 직접 가리킨다. 가장 흔한 것은 `sets.target`의 축 이름 오타다(`차종 축` 을 `차종축` 으로 적는 등).

- [ ] **Step 4: 채점기 자체 검증과 자기 채점**

```bash
python score_ses.py --selftest
python score_ses.py --ref reference-ses.json --runs reference-ses.json
```

기대: 자체 검증 6/6 통과. 자기 채점은 `nodes` `(62/62)` · `relations` `(61/61)` · `attrs` `(108/108)` 전부 `F1 1.000`, 임계점 `OCP-1 OCP-2 OCP-3 OCP-4 OCP-5`, **`근거 없는 노드 1개`**.

`근거 없는 노드`가 **1개인 것이 맞다.** 그 하나는 `생활쓰레기 수거 모델`이고, v7의 `대상 시스템`이
정확 키 근거 없이 속성 근거 둘(`1인배출량` · `배출량변동`)만 갖고 있던 것을 그대로 물려받은 것이다.
v8이 만든 구멍이 아니므로 이 계획에서 메우지 않는다 — 새 근거를 지어내지 않는다. Task 7에서
미검증 항목으로 기록한다.

**2개 이상이면** 새 엔티티 중 근거가 빠진 것이 있다. 어느 것인지는 `check_ref_v8.py all` 이
이름으로 알려준다.

- [ ] **Step 5: 커밋**

```bash
cd /c/Dev/MCP
git add waste-sim-spring/docs/research/s1-ses-extraction/reference-ses.json \
        waste-sim-spring/docs/research/s1-ses-extraction/check_ref_v8.py
git commit -m "$(cat <<'EOF'
feat(ses): 구성 결정 29개를 설정 가지로 올리고 sets로 잇는다

대응표에만 있던 "이 결정이 어디에 앉는가"를 트리로 승격한다. 새 주장을
만들지 않았다 — 29행을 그대로 옮겼고 근거도 그 표에서 왔다.

설정 엔티티는 모델 엔티티를 다시 소유하지 않는다. 부모가 둘이 되기
때문이다. 축을 고르는 결정은 선택자 속성 하나로만 적는다.

문항 24 교통모드만 속성이 아니라 결합의 active_when을 가리킨다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: 별칭 사전

과거 실행이 옛 이름을 내도 계속 매칭되게 한다. 그리고 `SimulationConfig`를 건너뛴 사유가 사라졌으므로 `_skipped`에서 꺼낸다.

**Files:**
- Modify: `aliases.json`

**Interfaces:**
- Consumes: Task 5까지의 `reference-ses.json` (v8 완성)
- Produces: `aliases.json` — 개명 3건 별칭 추가, 옛 이름을 가리키던 별칭 3건 재겨냥, `SimulationConfig → 시뮬레이션 설정` 승격, `_skipped.SimulationConfig` 삭제

**먼저 알아야 할 것 — 별칭은 한 단계만 풀린다.** `norm()`은 `aliases.get(s, s)` 를 한 번만 부른다
(`score_ses.py:28`). 그래서 `Scenario → 실험` 과 `실험 → 시나리오 실험` 을 함께 두면 `Scenario` 는
`실험` 에서 멈추고, 참조의 `실험` 은 `시나리오 실험` 까지 가서 **둘이 어긋난다.** 옛 이름을 값으로
가진 별칭은 새 이름으로 다시 겨눠야 한다. Step 3이 그 일이다.

- [ ] **Step 1: 지금 무엇이 미매칭인지 본다**

```bash
python score_ses.py --ref reference-ses.json --runs 'runs/gpt-4.1-mini-C*.json' 2>&1 | sed -n '/이름 미매칭/,$p'
```

기대: `참조에만 있음` 목록에 `시뮬레이션 설정` 등 새 엔티티 11개가, `추출에만 있음` 목록에 `대상 시스템` · `실험` · `관측` 이 보인다. 후자 셋이 Step 2로 사라진다.

- [ ] **Step 2: 별칭을 넣는다**

`aliases.json`에서 `"쓰레기통": "수거지점",` 바로 **앞**에 아래 세 줄을 넣는다.

```json
  "대상 시스템": "생활쓰레기 수거 모델",
  "실험": "시나리오 실험",
  "관측": "시뮬레이션 결과",
  "SimulationConfig": "시뮬레이션 설정",
```

- [ ] **Step 3: 옛 이름을 가리키던 별칭을 새 이름으로 다시 겨눈다**

`aliases.json`에서 아래 세 항목의 **값**을 바꾼다. 이걸 빼먹으면 과거 12건의 점수가 떨어지고
동결이 무너진다 — Step 5가 잡지만, 원인을 그때 찾는 것보다 지금 하는 편이 낫다.

```json
  "Scenario": "시나리오 실험",
  "SimulationScenario": "시나리오 실험",
  "SimulationResult": "시뮬레이션 결과",
```

값이 옛 이름인 것이 더 없는지 확인한다:

```bash
python -c "
import io,json
a=json.load(io.open('aliases.json',encoding='utf-8'))
bad=[(k,v) for k,v in a.items()
     if not k.startswith('_') and str(v).split('.')[0] in ('대상 시스템','실험','관측')]
print('옛 이름을 가리키는 별칭:', bad or '없음')
"
```

기대: `없음`.

- [ ] **Step 4: `_skipped`에서 `SimulationConfig`를 지운다**

`aliases.json`의 `_skipped` 객체에서 아래 키·값 쌍을 통째로 지운다.

```json
"SimulationConfig": "참조에 대응 노드가 없다. 참조는 이 설정값들을 '무엇을 서술하는가'로 거주민·수거지점·폐기물 유형·실험에 나눠 붙인다. 한 노드로 대응시키면 분류 기준이 다른 것을 같다고 적는 셈이다",
```

`_built` 값 끝에 아래를 덧붙인다.

```
 · 2026-09-16 ref-v8 재편으로 개명 3건을 넣고, 옛 이름을 가리키던 Scenario·SimulationScenario·SimulationResult를 새 이름으로 다시 겨눴다(별칭은 한 단계만 풀린다). SimulationConfig는 _skipped에서 꺼냈다 — v8에 시뮬레이션 설정이라는 대응 노드가 생겨 건너뛴 사유가 사라졌다
```

- [ ] **Step 5: JSON이 깨지지 않았는지, 별칭이 먹는지 확인한다**

```bash
python -c "import io,json; a=json.load(io.open('aliases.json',encoding='utf-8')); print(a['대상 시스템'], a['SimulationConfig']); print('SimulationConfig' in a['_skipped'])"
```

기대: `생활쓰레기 수거 모델 시뮬레이션 설정` 과 `False`.

```bash
python score_ses.py --ref reference-ses.json --runs 'runs/gpt-4.1-mini-C*.json' 2>&1 | sed -n '/이름 미매칭/,$p'
```

기대: `추출에만 있음` 목록에서 `대상 시스템` · `실험` · `관측` 이 사라졌다. `참조에만 있음`의 새 엔티티 11개는 남아 있다 — 추출기가 낼 수 없는 구조이므로 **의도한 것이다.**

- [ ] **Step 6: 동결본으로 잰 점수가 그대로인지 확인한다**

```bash
python score_ses.py --ref reference-ses-v7.json --runs 'runs/gpt-4.1-mini-C*.json' > /tmp/after-v7.txt
diff /tmp/baseline-v7.txt /tmp/after-v7.txt
```

기대: `diff`가 아무것도 내지 않는다(`v7 점수 불변 OK` 가 출력되게 아래처럼 돌려도 된다).

```bash
diff <(grep -E "P .* R .* F1" /tmp/baseline-v7.txt)      <(grep -E "P .* R .* F1" /tmp/after-v7.txt) && echo "v7 점수 불변 OK"
```

**P·R·F1이 한 줄이라도 달라지면 동결에 실패한 것이다.** 가장 흔한 원인은 Step 3을 빼먹은
것이다 — 별칭이 한 단계만 풀리므로 `Scenario` 가 `실험` 에서 멈춘다. 그다음으로 흔한 것은
`reference-ses-v7.json` 이 실수로 바뀐 것이다.

```bash
git diff --stat HEAD -- waste-sim-spring/docs/research/s1-ses-extraction/reference-ses-v7.json
```

기대: 아무것도 출력되지 않는다.

- [ ] **Step 7: 커밋**

```bash
cd /c/Dev/MCP
git add waste-sim-spring/docs/research/s1-ses-extraction/aliases.json
git commit -m "$(cat <<'EOF'
chore(ses): 별칭을 새 이름으로 다시 겨눈다

과거 실행이 대상 시스템·실험·관측을 내도 v8에서 계속 매칭된다.

별칭은 한 단계만 풀린다. Scenario가 실험을 가리킨 채로 실험→시나리오
실험을 더하면 Scenario가 실험에서 멈춰 어긋난다. 옛 이름을 값으로 가진
셋을 새 이름으로 다시 겨눴다.

SimulationConfig는 v7에서 "참조에 대응 노드가 없다"는 이유로 건너뛴
것이었다. v8에는 시뮬레이션 설정이 있으므로 그 사유가 사라졌다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: 문서 — 대응표와 검증 기록

트리가 바뀌었으므로 트리를 설명하는 문서가 어긋났다. 어긋난 문서는 없는 문서보다 나쁘다.

**Files:**
- Modify: `구성결정-SES-대응표.md`
- Modify: `참조-검증-기록.md`

**Interfaces:**
- Consumes: Task 5의 `reference-ses.json`, Task 6의 `aliases.json`
- Produces: 없음 (문서만)

- [ ] **Step 1: 대응표의 머리말을 갱신한다**

`구성결정-SES-대응표.md` 3행을 바꾼다.

```markdown
2026-09-16 · 문항 세트 `jangnyang-simulator-v4`(34문항) · 참조 `ref-v8` · 코드 `develop @ 12937be`
```

`## 표` 절의 안내문(`—`는 SES에 넣지 않는 것…) 아래에 한 문단을 넣는다.

```markdown
ref-v8부터 이 표는 트리에도 있다. 각 행의 `SES 자리`는 `reference-ses.json`의 설정 엔티티가
갖는 `sets[속성].target`과 같다. 둘이 어긋나면 `python check_ref_v8.py settings`가 잡는다 —
표와 트리 중 어느 쪽이 맞는지는 코드를 다시 보고 정한다.
```

- [ ] **Step 2: 표의 `SES 자리` 열에서 개명된 이름을 고친다**

아래 세 행만 바뀐다. 나머지 31행은 손대지 않는다.

| # | 지금 | 바꿀 것 |
|---:|---|---|
| 2 | `` `실험` **spec 실험 유형 축** `` | `` `시나리오 실험` **spec 실험 유형 축** · 설정: `실험 설정.실험유형` `` |
| 7 | `` `실험.기간` `` | `` `시나리오 실험.기간` · 설정: `실험 설정.기간` `` |
| 8 | `` `실험.반복횟수` `` | `` `시나리오 실험.반복횟수` · 설정: `실험 설정.반복횟수` `` |
| 9 | `` `대상 시스템.1인배출량` — 거주민별이 아니라 전역값이다 `` | `` `생활쓰레기 수거 모델.1인배출량` — 거주민별이 아니라 전역값이다 · 설정: `거주민·배출 설정.1인배출량` `` |
| 10 | `` `대상 시스템.배출량변동` **(신설)** — … `` | `` `생활쓰레기 수거 모델.배출량변동` — … · 설정: `거주민·배출 설정.배출량변동` `` |

나머지 24개 행에도 `· 설정: <설정 엔티티>.<속성>` 을 같은 방식으로 덧붙인다. 대응은
`reference-ses.json`의 `sets`에서 그대로 읽는다 — 손으로 짝짓지 않는다:

```bash
python -c "
import io,json
r=json.load(io.open('reference-ses.json',encoding='utf-8'))
for o,b in r['entities'].items():
    for a,v in (b.get('sets') or {}).items():
        print('%-20s %-14s -> %s' % (o, a, v['target']))
"
```

- [ ] **Step 3: 맺음말의 수를 확인한다**

`**앉은 것 29개 · 넣지 않은 것 5개.**` 는 그대로다. 아래를 덧붙인다.

```markdown
ref-v8에서 29개 모두가 `시뮬레이션 설정` 아래 다섯 설정 엔티티의 속성이 되었고, `sets`가
각각을 모델 트리의 자리에 잇는다. 넣지 않은 5개는 여전히 트리에 없다.
```

- [ ] **Step 4: 검증 기록에 재편을 적는다**

`참조-검증-기록.md` 끝에 아래 절을 넣는다.

```markdown
## ref-v7 → ref-v8 재편 (2026-09-16 · `develop @ 12937be`)

설계는 `docs/superpowers/specs/2026-09-16-ses-v8-tree-design.md` 에 있다.

### 바꾼 것

- 최상위를 `시스템 구성` 골격으로 바꿨다. 개명 3건 — `대상 시스템`→`생활쓰레기 수거 모델` ·
  `실험`→`시나리오 실험` · `관측`→`시뮬레이션 결과`
- 모델 가지의 자식 7개를 `DEVS 모델 구성` 5종 아래로 내렸다
- `시뮬레이션 설정` 가지를 세우고 대응표의 29개 결정을 `sets`로 이었다
- 엔티티 51 → 62개. 사라진 것은 없다

### 새 엔티티 11개의 근거

다섯 시스템(`거주민 배출 시스템` 등)의 근거는 **묶음의 근거**다. 새 개체를 주장하지 않고,
이미 근거가 있는 개체들이 코드에서 한 덩어리로 돈다는 사실을 가리킨다. 여섯 설정
(`시뮬레이션 설정` · 설정 5종)의 근거는 `JangnyangScenarioBuilder.toConfig():193`의
해당 구간과 `SimulationConfig:10`이다.

### 바꾸지 않은 것

- `couplings` 6건 · `critical_points` 5건의 내용. 개명에 걸린 이름만 옮겼다
- `evidence` 87건의 값. 키 7개만 개명했다
- 잎 값 — 직업 축 5종 · 실험 유형 축 13종 · 결과 구성 6종. 손으로 그린 그림은 각각
  3종 · 6묶음 · 5묶음이었는데 그것은 교정 **이전** 판이다
- `score_ses.py`. 이름에 의존하지 않아 한 줄도 고치지 않았다

### 넣지 않은 것 — 엔진과 이벤트

그림에 있는 `엔진 실행 ─ DEVS 엔진`과 `이벤트 실행 시스템 ─ 이벤트 집합`을 넣지 않았다.
ref-v7의 `_meta.note`가 "실행 엔진·이벤트 시스템을 구조 트리의 형제로 둔 명백한 오류"라고
적은 판단을 유지한다. `DischargeEvt` · `CollectEvt` · `InspectEvt`는 엔티티가 아니라 결합의
기제로 남아 있다 — `couplings` 6건의 `mechanism`에 이미 그렇게 적혀 있다.

### 별칭 사전의 변화

`SimulationConfig`를 `_skipped`에서 꺼내 `시뮬레이션 설정`으로 등재했다. v7에서 건너뛴 사유는
"참조에 대응 노드가 없다"였는데, v8에 그 노드가 생겨 사유가 사라졌다.

`Scenario` · `SimulationScenario` · `SimulationResult` 의 값을 새 이름으로 다시 겨눴다. 별칭은
`norm()`에서 한 단계만 풀리므로(`score_ses.py:28`), 옛 이름을 가리킨 채 두면 추출 결과가 옛
이름에서 멈추고 참조는 새 이름까지 가서 어긋난다. 동결본으로 잰 과거 12건의 점수가 재편 전과
같은 것을 확인했다.

### 아직 미검증

- 다섯 시스템의 경계가 DEVS 결합 모델의 원자 모델 경계와 일치하는지는 확인하지 않았다.
  v8은 분해 구조만 주장하고 포트를 주장하지 않는다
- `sets`의 28개 `target`이 실재하는 자리를 가리키는 것은 `check_ref_v8.py settings`가
  기계로 확인한다. 그 자리가 **의미상 맞는지**는 대응표의 코드 대조(2026-09-11)에 기댄다.
  그 뒤 코드가 바뀌었는지는 다시 보지 않았다
- `couplings`의 포트 이름과 CP-4 소속은 v7에서와 마찬가지로 미검증이다
- `생활쓰레기 수거 모델`에는 **엔티티 자체의 근거가 없다.** 속성 근거 둘(`1인배출량` ·
  `배출량변동`)만 있다. v7의 `대상 시스템`이 그랬고 개명이 그대로 물려받았다. `score_ses.py`의
  `근거 없는 노드`가 1로 나오는 것은 이 때문이고, 재편이 만든 구멍이 아니다. 없는 근거를
  지어내지 않았으므로 그대로 남긴다 — 메우려면 코드를 다시 봐야 한다.
  (v7 `_meta.status`의 "모든 엔티티가 코드 근거를 갖는다(51개)"는 속성 근거까지 셈한 말이다)
```

- [ ] **Step 5: 표와 트리가 어긋나지 않는지 기계로 확인한다**

```bash
python check_ref_v8.py all; echo "exit=$?"
```

기대: `[all] 통과` · `exit=0`. 문서만 고쳤으므로 통과해야 한다. 실패하면 Step 2에서 JSON을 실수로 건드린 것이다.

- [ ] **Step 6: 커밋**

```bash
cd /c/Dev/MCP
git add waste-sim-spring/docs/research/s1-ses-extraction/구성결정-SES-대응표.md \
        waste-sim-spring/docs/research/s1-ses-extraction/참조-검증-기록.md
git commit -m "$(cat <<'EOF'
docs(ses): 대응표와 검증 기록을 ref-v8에 맞춘다

트리가 바뀌었으므로 트리를 설명하는 문서가 어긋났다. 대응표의 29행은
이제 트리의 sets와 같은 것을 말하고, 둘이 어긋나면 검사기가 잡는다.

새 엔티티 11개의 근거와 미검증으로 남은 것을 검증 기록에 적는다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: 그림 재생성과 최종 검증

`render_ses.py`는 루트에서 트리를 걷기 때문에 코드를 고치지 않는다. 그림만 다시 뽑고, 전체를 한 번에 확인한다.

**Files:**
- Modify: `ses-tree.html` · `ses-tree.png` · `ses-mapping.html` · `ses-mapping.png` (생성물)

**Interfaces:**
- Consumes: Task 1–7의 모든 산출물
- Produces: 없음 (최종 검증)

- [ ] **Step 1: 그림을 다시 뽑는다**

```bash
python render_ses.py --png
```

기대: 네 파일이 갱신된다. Chrome 경로(`render_ses.py:14`, `C:\Program Files\Google\Chrome\Application\chrome.exe`)가 없으면 PNG 단계만 실패한다. 그때는 HTML만 만든다:

```bash
python render_ses.py
```

PNG를 못 뽑았으면 그 사실을 최종 보고에 적는다. **뽑았다고 말하지 않는다.**

- [ ] **Step 2: 그림이 새 골격인지 눈으로 확인한다**

```bash
python -c "
import io
h=io.open('ses-tree.html',encoding='utf-8').read()
for k in ['시스템 구성','시뮬레이션 설정','DEVS 모델 구성','거주민 배출 시스템','시나리오 실험','시뮬레이션 결과']:
    print(('있음  ' if k in h else '없음  ')+k)
for k in ['대상 시스템','DEVS 엔진','이벤트 실행 시스템']:
    print(('!! 남아 있음  ' if k in h else '없음(맞음)  ')+k)
"
```

기대: 앞 여섯은 `있음`, 뒤 셋은 `없음(맞음)`.

- [ ] **Step 3: 전체 검증을 한 번에 돌린다**

```bash
python check_ref_v8.py all;                                        echo "1) exit=$?"
python score_ses.py --selftest;                                    echo "2) exit=$?"
python score_ses.py --ref reference-ses.json --runs reference-ses.json | grep -E "nodes|relations|attrs|근거 없는|임계점"
python score_ses.py --ref reference-ses-v7.json --runs 'runs/gpt-4.1-mini-C*.json' > /tmp/final-v7.txt
diff <(grep -E "P .* R .* F1" /tmp/baseline-v7.txt) <(grep -E "P .* R .* F1" /tmp/final-v7.txt) && echo "3) v7 점수 불변"
python -c "
import io,json
r=json.load(io.open('reference-ses.json',encoding='utf-8'))
print('엔티티', len(r['entities']), '· 결합', len(r['couplings']),
      '· 임계점', len(r['critical_points']), '· 근거', len(r['evidence']),
      '· version', r['_meta']['version'])
"
```

기대:
1. `[all] 통과` · `exit=0`
2. `자체 검증 6/6 통과` · `exit=0`
3. 자기 채점 `nodes (62/62)` · `relations (61/61)` · `attrs (108/108)` 모두 `F1 1.000`, 임계점 `OCP-1 OCP-2 OCP-3 OCP-4 OCP-5`, `근거 없는 노드 1개`(아래 설명대로 맞는 값)
4. `3) v7 점수 불변` 출력 — 동결본으로 잰 과거 12건의 P·R·F1이 Task 1의 기준선과 한 줄도 다르지 않다
5. `엔티티 62 · 결합 6 · 임계점 5 · 근거 98 · version ref-v8`

**4번이 다르면 동결에 실패한 것이다.** 재편의 전제가 무너지므로 멈추고 보고한다.

- [ ] **Step 4: 임시 파일을 지운다**

```bash
rm -f /tmp/step3.py /tmp/step4.py /tmp/step5.py /tmp/baseline-v7.txt /tmp/after-v7.txt /tmp/final-v7.txt
```

- [ ] **Step 5: 커밋**

```bash
cd /c/Dev/MCP
git add waste-sim-spring/docs/research/s1-ses-extraction/ses-tree.html \
        waste-sim-spring/docs/research/s1-ses-extraction/ses-tree.png \
        waste-sim-spring/docs/research/s1-ses-extraction/ses-mapping.html \
        waste-sim-spring/docs/research/s1-ses-extraction/ses-mapping.png
git commit -m "$(cat <<'EOF'
docs(ses): 새 골격으로 트리 그림을 다시 뽑는다

렌더러는 루트에서 걷기 때문에 코드를 고치지 않았다. 손으로 그리지
않는다 — 곧 어긋난다.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 6: 최종 보고**

아래를 사람에게 보고한다. 확인하지 않은 것을 확인했다고 적지 않는다.

- 엔티티 51 → 62개, `_meta.version` = `ref-v8`
- `check_ref_v8.py all` 통과 · `score_ses.py --selftest` 6/6 통과 · 자기 채점 F1 1.0 · 근거 없는 노드 1개(v7에서 물려받은 것)
- 동결본으로 잰 과거 12건의 점수가 재편 전과 같다
- PNG를 뽑았는지 (Chrome이 없으면 HTML만)
- 미검증으로 남은 것 — `참조-검증-기록.md`의 "아직 미검증" 절
