# 흐름 기반 SES 추출 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 파이프라인이 배열의 색인 축 대신 값의 흐름을 읽게 한다 — 결합의 방향을 코드가 만들고, 트리 모양을 계약으로 막고, 루트를 사람 몫으로 되돌린다.

**Architecture:** 결합 질문을 e1(자리)·e2(주체)로 쪼개고, 새 순수 모듈 `flow.py` 가 `쓰는 자리 × 읽는 자리` 를 짝지어 방향 있는 결합을 만든다. 트리 모양은 새 순수 모듈 `shape.py` 의 판정표로 조립기가 집행한다. 루트는 추출에서 빠지고 `review.py` 의 새 판정으로 들어온다.

**Tech Stack:** Python 3.14, 표준 라이브러리만. 시험은 `unittest`. 외부 의존성 없음(`contract.py` 의 기존 방침).

**Spec:** `docs/superpowers/specs/2026-09-16-ses-흐름기반-추출-design.md`

## Global Constraints

- 작업 디렉터리는 `waste-sim-spring/docs/research/s1-ses-extraction/` 이다. 아래 모든 경로와 명령은 이 디렉터리 기준이다.
- **기존 조건을 건드리지 않는다.** `run_s1.py`, `run_t.py`, `runs/`, 그리고 `stages.PLANS` 의 기존 항목(`T2`, `T2b`, `T2c`, `T2d`, `T2-tree`, `T2-noroot`, `S2`, `T3-candidates`)은 수정 대상이 아니다. 새 계획 이름은 `T2-flow` 다.
- **외부 의존성을 추가하지 않는다.** `contract.py`·`shape.py`·`flow.py` 는 표준 라이브러리만 쓴다.
- **조용한 삭제가 없다.** 버리는 대신 `unresolved`(보류) 또는 `provenance`(판정 기록)로 옮기고 `origin_raw` 를 단다.
- **모르는 것은 `unknown` 이다.** 기본값을 채워 넣지 않는다.
- 주석·문서·설명 문구는 한국어로 쓴다. 기존 파일의 문체를 따른다.
- 커밋 메시지는 한국어 한 줄 요약에 `docs(ses):`/`feat(ses):`/`fix(ses):` 접두를 붙인다. 기존 이력과 같은 형식이다.
- 전체 시험은 `python -m unittest discover -s tests` 로 돈다. 현재 122건이 통과한다 — **어느 과제도 기존 시험을 깨지 않는다.**

---

### Task 1: 계약 확장 — 자기결합·축 이름·파생 출처

계약에 세 가지를 더한다. 결합의 자기참조를 불합격으로 만들고(현재 분해에만 있다), 분해에 축 이름 `label` 을 두고, 결합이 어느 자리에서 나왔는지 `derived_from` 으로 남긴다. 모양 위반을 가리킬 보류 사유 코드도 여기서 더한다.

**Files:**
- Modify: `sesx/contract.py:14` (`SCHEMA_VERSION`), `sesx/contract.py:24-31` (`REASON_CODES`), `sesx/contract.py:195-207` (참조 검사)
- Test: `tests/test_contract_flow.py` (새로 만든다)

**Interfaces:**
- Consumes: 없음 (첫 과제)
- Produces:
  - `contract.SCHEMA_VERSION == "ses-extraction/1.1"`
  - `contract.REASON_CODES` 에 `"shape_violation"` 포함
  - `contract.validate_artifact(doc)` 가 `source.entity_id == target.entity_id` 인 결합에 대해 `{"code": "SELF_COUPLING", "layer": "reference", ...}` 를 낸다

- [ ] **Step 1: 실패하는 시험을 쓴다**

`tests/test_contract_flow.py` 를 만든다:

```python
# -*- coding: utf-8 -*-
import unittest
from sesx import contract


def doc_with_coupling(source_eid, target_eid):
    """개체 하나짜리 최소 산출물에 결합 하나를 단다."""
    doc = contract.new_artifact("a1")
    doc["entities"] = [
        {"id": "E1", "name": "수거", "kind": "stateful", "scope": "simulation_target",
         "status": "proposed", "evidence_ids": []},
        {"id": "E2", "name": "넘침판정", "kind": "stateful", "scope": "simulation_target",
         "status": "proposed", "evidence_ids": []},
    ]
    doc["activation"] = [{"id": "A1", "state": "unknown", "clauses": [],
                          "combination": None, "coverage": "incomplete",
                          "status": "proposed", "evidence_ids": []}]
    doc["couplings"] = [{
        "id": "C1",
        "source": {"entity_id": source_eid, "attribute_id": None, "symbol": None},
        "target": {"entity_id": target_eid, "attribute_id": None, "symbol": None},
        "payload": {"kind": "shared_state", "code_expression": "fill[b][t]",
                    "meaning": None},
        "mechanism": None, "activation_id": "A1",
        "derived_from": [], "evidence_ids": [], "status": "proposed"}]
    return doc


class SelfCouplingTests(unittest.TestCase):
    def codes(self, doc):
        return [f["code"] for f in contract.validate_artifact(doc)]

    def test_자기결합은_불합격이다(self):
        doc = doc_with_coupling("E1", "E1")
        self.assertIn("SELF_COUPLING", self.codes(doc))

    def test_서로_다른_끝점은_통과한다(self):
        doc = doc_with_coupling("E1", "E2")
        self.assertNotIn("SELF_COUPLING", self.codes(doc))


class SchemaTests(unittest.TestCase):
    def test_스키마_판이_올랐다(self):
        self.assertEqual(contract.SCHEMA_VERSION, "ses-extraction/1.1")

    def test_모양_위반_사유가_있다(self):
        self.assertIn("shape_violation", contract.REASON_CODES)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 시험이 실패하는지 본다**

Run: `python -m unittest tests.test_contract_flow -v`
Expected: FAIL — `SELF_COUPLING` 이 목록에 없고, `SCHEMA_VERSION` 이 `ses-extraction/1.0` 이며, `shape_violation` 이 `REASON_CODES` 에 없다.

- [ ] **Step 3: 계약을 고친다**

`sesx/contract.py:14` 의 판을 올린다:

```python
SCHEMA_VERSION = "ses-extraction/1.1"
```

`REASON_CODES` 끝에 사유를 더한다:

```python
    "payload_unknown", "not_reviewed", "unproven_ownership", "not_an_entity",
    "shape_violation",
)
```

`_check_references` 의 결합 반복문(`for c in doc.get("couplings") or []:`)에 자기참조 검사를 더한다. 분해의 `SELF_REFERENCE` 와 같은 자리에 둔다:

```python
    for c in doc.get("couplings") or []:
        for side in ("source", "target"):
            box = c.get(side) or {}
            need(box.get("entity_id"), ent, "ENTITY_REF_BROKEN", c.get("id"),
                 f"{side}.entity_id")
            need(box.get("attribute_id"), att, "ATTRIBUTE_REF_BROKEN", c.get("id"),
                 f"{side}.attribute_id")
        se = (c.get("source") or {}).get("entity_id")
        te = (c.get("target") or {}).get("entity_id")
        if se and te and se == te:
            failures.append(fail("SELF_COUPLING",
                                 f"{c.get('id')} 출발과 도착이 같은 개체다",
                                 [c.get("id")], "reference"))
        need(c.get("activation_id"), act, "ACTIVATION_REF_BROKEN", c.get("id"),
             "activation_id")
```

- [ ] **Step 4: 시험이 통과하는지 본다**

Run: `python -m unittest tests.test_contract_flow -v`
Expected: PASS (4건)

- [ ] **Step 5: 기존 시험이 깨지지 않았는지 본다**

Run: `python -m unittest discover -s tests`
Expected: OK. `SCHEMA_VERSION` 을 문자열로 박아 둔 시험이 있으면 새 값으로 고친다 — 확인: `grep -rn "ses-extraction/1.0" tests/ sesx/`

- [ ] **Step 6: 커밋**

```bash
git add sesx/contract.py tests/test_contract_flow.py
git commit -m "feat(ses): 자기결합을 계약 위반으로 잡는다"
```

---

### Task 2: 모양 계약 모듈 `shape.py`

ref-v8 이 쓰는 세 층만 합법으로 두는 판정표를 순수 함수로 만든다. 조립기는 다음 과제에서 이 함수를 부른다. 여기서는 판정만 만들고 집행하지 않는다.

**Files:**
- Create: `sesx/shape.py`
- Test: `tests/test_shape.py`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `shape.edge_error(parent_kind: str, decomp_kind: str, member_kinds: list[str]) -> str | None` — 위반 문구, 합법이면 `None`
  - `shape.shared_declaration(member_evidence: dict, members: list[str]) -> tuple[str, str] | None` — 근거가 같은 선언 행을 가리키는 두 자식 이름, 없으면 `None`

- [ ] **Step 1: 실패하는 시험을 쓴다**

`tests/test_shape.py` 를 만든다:

```python
# -*- coding: utf-8 -*-
import unittest
from sesx import shape


def ev(line, path="SimulationEngine.java"):
    return [{"file_path": path, "start_line": line, "end_line": line, "quote": "q"}]


class EdgeTests(unittest.TestCase):
    def test_boundary_는_aspect_로_집합을_갖는다(self):
        self.assertIsNone(shape.edge_error("boundary", "ASPECT", ["set", "set"]))

    def test_boundary_는_aspect_로_경계를_갖는다(self):
        self.assertIsNone(shape.edge_error("boundary", "ASPECT", ["boundary"]))

    def test_집합은_multi_로_개체_하나를_낳는다(self):
        self.assertIsNone(shape.edge_error("set", "MULTI", ["stateful"]))

    def test_개체는_spec_으로_유형을_가른다(self):
        self.assertIsNone(shape.edge_error("stateful", "SPEC", ["type", "type"]))

    def test_multi_자식이_둘이면_위반이다(self):
        self.assertIsNotNone(shape.edge_error("set", "MULTI", ["stateful", "stateful"]))

    def test_개체가_multi_부모면_위반이다(self):
        # 운행 --MULTI--> 트럭. 운행은 set 이 아니다
        self.assertIsNotNone(shape.edge_error("stateful", "MULTI", ["stateful"]))

    def test_aspect_자식에_유형이_오면_위반이다(self):
        self.assertIsNotNone(shape.edge_error("boundary", "ASPECT", ["type"]))

    def test_집합이_spec_부모면_위반이다(self):
        self.assertIsNotNone(shape.edge_error("set", "SPEC", ["type"]))

    def test_역할을_모르면_막지_않는다(self):
        # unknown 은 판정을 미룬다. 막는 것은 확인된 위반뿐이다
        self.assertIsNone(shape.edge_error("unknown", "ASPECT", ["unknown"]))


class DeclarationTests(unittest.TestCase):
    def test_같은_선언_행을_가리키면_쌍을_낸다(self):
        me = {"건물": ev(254), "쓰레기 종류": ev(254)}
        self.assertEqual(shape.shared_declaration(me, ["건물", "쓰레기 종류"]),
                         ("건물", "쓰레기 종류"))

    def test_다른_행이면_없다(self):
        me = {"건물": ev(254), "쓰레기 종류": ev(255)}
        self.assertIsNone(shape.shared_declaration(me, ["건물", "쓰레기 종류"]))

    def test_같은_행이라도_파일이_다르면_없다(self):
        me = {"건물": ev(254, "A.java"), "쓰레기 종류": ev(254, "B.java")}
        self.assertIsNone(shape.shared_declaration(me, ["건물", "쓰레기 종류"]))

    def test_자식이_하나면_없다(self):
        self.assertIsNone(shape.shared_declaration({"건물": ev(254)}, ["건물"]))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 시험이 실패하는지 본다**

Run: `python -m unittest tests.test_shape -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'sesx.shape'`

- [ ] **Step 3: `sesx/shape.py` 를 쓴다**

```python
# -*- coding: utf-8 -*-
"""트리 모양 계약. ref-v8 이 쓰는 세 층만 합법으로 둔다.

    boundary ─ASPECT→ boundary · set
    set      ─MULTI → 개체 하나(stateful)
    stateful ─SPEC  → type

판정만 한다. 집행은 조립기가 보류로 한다 — 계약 위반으로 올리지 않는 이유는
거짓 양성이 승인 경로 전체를 막기 때문이다.

역할이 `unknown` 이면 막지 않는다. 확인된 위반만 잡는다.
"""
from __future__ import annotations

# (부모 역할, 분해 종류) -> 허용되는 자식 역할
LEGAL_MEMBERS = {
    ("boundary", "ASPECT"): ("boundary", "set"),
    ("set", "MULTI"): ("stateful",),
    ("stateful", "SPEC"): ("type",),
}

UNKNOWN = "unknown"


def edge_error(parent_kind, decomp_kind, member_kinds):
    """모양 계약 위반 문구. 합법이거나 판정할 수 없으면 None."""
    kind = str(decomp_kind or "").upper()
    if parent_kind == UNKNOWN or not parent_kind:
        return None
    allowed = LEGAL_MEMBERS.get((parent_kind, kind))
    if allowed is None:
        legal = [f"{p} ─{k}→" for (p, k) in LEGAL_MEMBERS if p == parent_kind]
        return (f"{parent_kind} 부모에 {kind} 분해는 모양 계약에 없다. "
                f"허용: {legal or '없음'}")
    if kind == "MULTI" and len(member_kinds) != 1:
        return f"MULTI 는 개체 하나를 복제한다. 자식 {len(member_kinds)}개를 받았다"
    bad = [k for k in member_kinds if k != UNKNOWN and k and k not in allowed]
    if bad:
        return (f"{parent_kind} ─{kind}→ 의 자식은 {list(allowed)} 여야 한다. "
                f"받은 역할: {bad}")
    return None


def _sites(evidence):
    out = set()
    for e in evidence or []:
        if not isinstance(e, dict):
            continue
        path, start = e.get("file_path"), e.get("start_line")
        if isinstance(path, str) and isinstance(start, int):
            out.add((path, start, e.get("end_line", start)))
    return out


def shared_declaration(member_evidence, members):
    """두 자식의 역할 근거가 **같은 선언 행**을 가리키면 그 두 이름.

    `new double[nB][nT]` 한 줄을 두 자식의 근거로 쓴 경우다. 같은 선언의 서로
    다른 차원은 형제이지 부모-자식이 아니다.
    """
    names = [m for m in (members or []) if isinstance(m, str)]
    for i, a in enumerate(names):
        sa = _sites((member_evidence or {}).get(a))
        if not sa:
            continue
        for b in names[i + 1:]:
            if sa & _sites((member_evidence or {}).get(b)):
                return (a, b)
    return None
```

- [ ] **Step 4: 시험이 통과하는지 본다**

Run: `python -m unittest tests.test_shape -v`
Expected: PASS (13건)

- [ ] **Step 5: 커밋**

```bash
git add sesx/shape.py tests/test_shape.py
git commit -m "feat(ses): 트리 모양 계약을 판정표로 적는다"
```

---

### Task 3: 조립기가 모양과 역할 근거를 집행한다

`assemble.py` 의 구멍 둘을 막고 `shape.py` 를 붙인다. 하나는 `member_evidence` 를 자식이 새 개체일 때만 요구하는 조건이고(자식 7개의 근거가 0건이었던 이유), 다른 하나는 모양 검사가 아예 없는 것이다. 분해의 축 이름 `label` 도 여기서 보관한다.

**Files:**
- Modify: `sesx/assemble.py:402-508` (`_decompositions`)
- Test: `tests/test_shape_enforcement.py`

**Interfaces:**
- Consumes: `shape.edge_error`, `shape.shared_declaration` (Task 2)
- Produces: 조립기가 낸 `doc["unresolved"]` 항목의 `reason_code` 로 `"shape_violation"` 을 쓴다. 살아남은 분해 항목에 `"label"` 키가 있다.

- [ ] **Step 1: 실패하는 시험을 쓴다**

`tests/test_shape_enforcement.py` 를 만든다:

```python
# -*- coding: utf-8 -*-
import unittest
from sesx import assemble, stages

EV = [{"file_path": "SimulationEngine.java", "start_line": 254, "end_line": 254,
       "quote": "double[][] fill = new double[nB][nT];"}]
EV2 = [{"file_path": "SimulationEngine.java", "start_line": 300, "end_line": 300,
        "quote": "trips.add(trip);"}]


def entity(name, kind="boundary"):
    return {"name": name, "kind": kind, "scope": "simulation_target",
            "identity_evidence": EV, "composition_evidence": EV}


def build(payload):
    return assemble.build(payload, "s", "a", {"strategy": "T2-flow"}, {},
                          no_new_entity_stages=stages.NO_NEW_ENTITY_STAGES)


def reasons(doc):
    return [u["reason_code"] for u in doc["unresolved"]]


class MemberEvidenceTests(unittest.TestCase):
    def test_기존_자식이어도_역할_근거를_요구한다(self):
        doc = build({"g": {"entities": [entity("수거장 적재 시스템"),
                                        entity("수거지점 집합", "set")]},
                     "d": {"decompositions": [
                         {"parent": "수거장 적재 시스템", "kind": "ASPECT",
                          "members": ["수거지점 집합"], "label": "적재 구성",
                          "evidence": EV}]}})
        self.assertIn("unknown_reference", reasons(doc))
        self.assertEqual(doc["decompositions"], [])


class ShapeTests(unittest.TestCase):
    def decomp(self, parent, kind, members, member_ev=None, label="축"):
        return {"parent": parent, "kind": kind, "members": members, "label": label,
                "evidence": EV2,
                "member_evidence": member_ev or {m: EV2 for m in members}}

    def test_개체가_multi_부모면_보류한다(self):
        doc = build({"g": {"entities": [entity("운행", "stateful"),
                                        entity("트럭", "stateful")]},
                     "d": {"decompositions": [self.decomp("운행", "MULTI", ["트럭"])]}})
        self.assertIn("shape_violation", reasons(doc))
        self.assertEqual(doc["decompositions"], [])

    def test_multi_자식이_둘이면_보류한다(self):
        doc = build({"g": {"entities": [entity("거주민 집합", "set"),
                                        entity("거주민", "stateful"),
                                        entity("방문객", "stateful")]},
                     "d": {"decompositions": [
                         self.decomp("거주민 집합", "MULTI", ["거주민", "방문객"])]}})
        self.assertIn("shape_violation", reasons(doc))

    def test_같은_선언_행을_가리키는_두_자식은_보류한다(self):
        doc = build({"g": {"entities": [entity("건물과 쓰레기 종류"),
                                        entity("건물", "set"), entity("쓰레기 종류", "set")]},
                     "d": {"decompositions": [
                         self.decomp("건물과 쓰레기 종류", "ASPECT", ["건물", "쓰레기 종류"],
                                     {"건물": EV, "쓰레기 종류": EV})]}})
        self.assertIn("shape_violation", reasons(doc))

    def test_합법한_분해는_통과하고_축_이름을_지킨다(self):
        doc = build({"g": {"entities": [entity("거주민 집합", "set"),
                                        entity("거주민", "stateful")]},
                     "d": {"decompositions": [
                         self.decomp("거주민 집합", "MULTI", ["거주민"],
                                     label="거주민 다중")]}})
        self.assertNotIn("shape_violation", reasons(doc))
        self.assertEqual(len(doc["decompositions"]), 1)
        self.assertEqual(doc["decompositions"][0]["label"], "거주민 다중")

    def test_보류해도_원문이_남는다(self):
        doc = build({"g": {"entities": [entity("운행", "stateful"),
                                        entity("트럭", "stateful")]},
                     "d": {"decompositions": [self.decomp("운행", "MULTI", ["트럭"])]}})
        held = [u for u in doc["unresolved"] if u["reason_code"] == "shape_violation"]
        self.assertTrue(held[0]["origin_raw"])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 시험이 실패하는지 본다**

Run: `python -m unittest tests.test_shape_enforcement -v`
Expected: FAIL — 모양 위반이 보류되지 않고 분해로 들어가며, 역할 근거 없는 분해가 통과하고, `label` 키가 없다.

- [ ] **Step 3: `_decompositions` 를 고친다**

`sesx/assemble.py` 머리에 `shape` 를 들인다:

```python
from . import contract, shape
```

(기존 import 줄을 확인하고 `shape` 만 더한다.)

`_decompositions` 안에서 `member_ev` 를 읽은 뒤, 기존의 `for m in members:` 반복문을 아래로 바꾼다. **`self._entity_id(m)` 로 빠져나가던 `continue` 를 없애는 것**이 핵심이다:

```python
            member_ev = _obj(d.get("member_evidence")) or {}
            created, missing = [], []
            for m in members:
                raws = member_ev.get(m) if isinstance(member_ev.get(m), list) else None
                if not raws:
                    # 자식이 이미 확정 개체여도 역할 근거를 요구한다.
                    # tree_rules.md 가 그렇게 적어 두었는데 집행되지 않고 있었다.
                    missing.append(m)
                    continue
                if self._entity_id(m):
                    continue
                if stage in self.no_new_entity_stages or (self.tree_mode and kind == "ASPECT"):
                    missing.append(m)
                    continue
                created.append((m, raws))
            if missing:
                self._hold(stage, raw_id, d, "unknown_reference",
                           f"자식 역할 근거 없음: {missing}",
                           "각 부모-자식 연결의 역할 근거가 필요하다. SPEC 은 하위 유형 근거, "
                           "MULTI 는 반복 인스턴스·범위 근거다.")
                self._prov(stage, raw_id, d, "held", "자식 역할 근거 없음")
                continue
```

같은 함수에서 자식 개체가 모두 확보된 뒤, 분해 항목을 만들기 **직전**에 모양 검사를 넣는다:

```python
            pair = shape.shared_declaration(member_ev, members)
            if pair:
                self._hold(stage, raw_id, d, "shape_violation",
                           f"{pair[0]} 와 {pair[1]} 의 역할 근거가 같은 선언 행이다",
                           "같은 선언의 서로 다른 차원은 형제다. 부모-자식으로 묶지 않는다.")
                self._prov(stage, raw_id, d, "held", "배열 차원을 부모-자식으로 접었다")
                continue
            parent_kind = self._kind_of(parent)
            member_kinds = [self._kind_of(self._entity_id(m)) for m in members]
            err = shape.edge_error(parent_kind, kind, member_kinds)
            if err:
                self._hold(stage, raw_id, d, "shape_violation", err,
                           "ref-v8 의 모양은 boundary ─ASPECT→ set ─MULTI→ 개체 ─SPEC→ 유형이다.")
                self._prov(stage, raw_id, d, "held", "트리 모양 계약 위반")
                continue
```

조립된 분해 항목에 `label` 을 넣는다 (`"kind": kind,` 바로 아래):

```python
                "label": d.get("label"),
```

역할을 찾는 도우미를 `_entity_id` 옆에 더한다:

```python
    def _kind_of(self, entity_id):
        """확정 개체의 역할. 못 찾으면 unknown — 모양 검사를 미룬다."""
        for e in self.doc["entities"]:
            if e["id"] == entity_id:
                return e.get("kind") or "unknown"
        return "unknown"
```

- [ ] **Step 4: 시험이 통과하는지 본다**

Run: `python -m unittest tests.test_shape_enforcement -v`
Expected: PASS (6건)

- [ ] **Step 5: 기존 시험을 돌린다**

Run: `python -m unittest discover -s tests`
Expected: `tests/test_tree.py` 의 몇 건이 깨질 수 있다 — `relation()` 이 `member_evidence` 를 이미 채우므로 대부분 통과하지만, 역할이 전부 `boundary` 인 고정물은 `boundary ─ASPECT→ boundary` 라 합법이다. **깨진 시험이 있으면 그 시험이 무엇을 주장하는지 읽고**, 새 계약이 옳으면 고정물을 ref-v8 모양으로 고친다. 옛 동작을 지키려고 집행을 약하게 만들지 않는다.

- [ ] **Step 6: 커밋**

```bash
git add sesx/assemble.py tests/test_shape_enforcement.py
git commit -m "feat(ses): 조립기가 모양 계약과 역할 근거를 집행한다"
```

---

### Task 4: `flow.py` — 자리 병합

b2 가 이미 낸 `state_evidence`(쓰기)·`consumption_evidence`(읽기)를 씨앗으로 삼고, e1 이 채운 자리를 합쳐 하나의 자리 목록을 만든다. 파생은 다음 과제다.

**Files:**
- Create: `sesx/flow.py`
- Test: `tests/test_flow.py`

**Interfaces:**
- Consumes: 없음 (순수 함수)
- Produces:
  - `flow.merge_sites(b2_payload: dict, e1_payload: dict) -> list[dict]`
  - 자리 목록 항목: `{"value": str, "declaration": dict|None, "writes": [site], "reads": [site]}`
  - `site`: `{"site_id": str, "file_path": str, "start_line": int, "end_line": int, "quote": str, "effect": str, "from": "b2"|"e1"}`
  - 자리 식별자 규약: `e1:v<값 순번>:w<n>` · `e1:v<값 순번>:r<n>` (0부터)

- [ ] **Step 1: 실패하는 시험을 쓴다**

`tests/test_flow.py` 를 만든다:

```python
# -*- coding: utf-8 -*-
import unittest
from sesx import flow


def ev(line, quote):
    return {"file_path": "SimulationEngine.java", "start_line": line,
            "end_line": line, "quote": quote}


B2 = {"subjects": [{
    "state": "fill[b][t]", "classification": "entity_state", "owner_candidate": "수거지점",
    "state_evidence": [ev(454, "fill[de.building][t] += add")],
    "identity_evidence": [ev(254, "double[][] fill = new double[nB][nT];")],
    "linkage_evidence": [ev(254, "double[][] fill = new double[nB][nT];")],
    "consumption_evidence": [ev(414, "dueTotal += fill[ce.building][t]")]}]}

E1 = {"value_sites": [{
    "value": {"code_expression": "fill[b][t]",
              "declaration": ev(254, "double[][] fill = new double[nB][nT];")},
    "writes": [ev(424, "fill[ce.building][t] *= keepFraction")],
    "reads": [ev(470, "worst = max(worst, fill[ie.building][t]/cap)")],
    "corrections": []}]}


class MergeTests(unittest.TestCase):
    def test_씨앗과_새_자리가_합쳐진다(self):
        sites = flow.merge_sites(B2, E1)
        self.assertEqual(len(sites), 1)
        self.assertEqual(len(sites[0]["writes"]), 2)
        self.assertEqual(len(sites[0]["reads"]), 2)

    def test_씨앗에_출처가_붙는다(self):
        sites = flow.merge_sites(B2, E1)
        froms = {w["start_line"]: w["from"] for w in sites[0]["writes"]}
        self.assertEqual(froms[454], "b2")
        self.assertEqual(froms[424], "e1")

    def test_자리마다_식별자가_있다(self):
        sites = flow.merge_sites(B2, E1)
        ids = [w["site_id"] for w in sites[0]["writes"]]
        self.assertEqual(ids, ["e1:v0:w0", "e1:v0:w1"])

    def test_같은_행을_두_번_내면_하나로_합친다(self):
        e1 = {"value_sites": [{
            "value": {"code_expression": "fill[b][t]", "declaration": None},
            "writes": [ev(454, "fill[de.building][t] += add")],
            "reads": [], "corrections": []}]}
        sites = flow.merge_sites(B2, e1)
        self.assertEqual(len(sites[0]["writes"]), 1)
        self.assertEqual(sites[0]["writes"][0]["from"], "b2")

    def test_정정된_씨앗은_빠진다(self):
        e1 = {"value_sites": [{
            "value": {"code_expression": "fill[b][t]", "declaration": None},
            "writes": [], "reads": [],
            "corrections": [{"site_id": "e1:v0:w0", "why": "이 값의 쓰기가 아니다"}]}]}
        sites = flow.merge_sites(B2, e1)
        self.assertEqual(sites[0]["writes"], [])

    def test_entity_state_가_아닌_값은_빠진다(self):
        b2 = {"subjects": [{"state": "total", "classification": "value",
                            "state_evidence": [ev(1, "total += 1")],
                            "consumption_evidence": []}]}
        self.assertEqual(flow.merge_sites(b2, {"value_sites": []}), [])

    def test_e1_이_비어도_씨앗만으로_선다(self):
        sites = flow.merge_sites(B2, {"value_sites": []})
        self.assertEqual(len(sites[0]["writes"]), 1)
        self.assertEqual(len(sites[0]["reads"]), 1)

    def test_효과를_모르면_unknown_이다(self):
        sites = flow.merge_sites(B2, {"value_sites": []})
        self.assertEqual(sites[0]["writes"][0]["effect"], "unknown")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 시험이 실패하는지 본다**

Run: `python -m unittest tests.test_flow -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'sesx.flow'`

- [ ] **Step 3: `sesx/flow.py` 를 쓴다**

```python
# -*- coding: utf-8 -*-
"""값 흐름에서 결합을 만든다. **모델이 참여하지 않는 단계다.**

b2 는 이미 상태 값마다 쓰는 자리(state_evidence)와 읽는 자리(consumption_evidence)를
따로 묻는다. e1 은 그 목록에서 빠진 자리를 채운다. 여기서 둘을 합치고(merge_sites),
`쓰는 자리 × 읽는 자리` 를 짝지어 방향 있는 결합을 만든다(derive).

짝짓기와 방향을 코드가 하므로 출발과 도착이 같은 결합이 나올 수 없다. 같은 주체끼리의
쌍은 결합이 아니라 상태 갱신이며, 버리지 않고 판정 기록으로 옮긴다.
"""
from __future__ import annotations

EFFECTS = ("increase", "decrease", "assign", "unknown")


def _site(raw, site_id, origin):
    """근거 하나를 자리 하나로. 형식이 어긋나면 None."""
    if not isinstance(raw, dict):
        return None
    path, start = raw.get("file_path"), raw.get("start_line")
    if not isinstance(path, str) or not path.strip() or not isinstance(start, int):
        return None
    effect = raw.get("effect")
    return {"site_id": site_id,
            "file_path": path,
            "start_line": start,
            "end_line": raw.get("end_line", start),
            "quote": raw.get("quote"),
            "effect": effect if effect in EFFECTS else "unknown",
            "from": origin}


def _key(site):
    return (site["file_path"], site["start_line"], site["end_line"])


def _value_expression(subject):
    return subject.get("state")


def merge_sites(b2_payload, e1_payload):
    """b2 씨앗과 e1 보충을 합친 자리 목록.

    같은 행을 둘 다 냈으면 하나로 합치고 **씨앗 쪽을 남긴다**(먼저 온 것이 출처다).
    e1 의 corrections 가 가리킨 자리는 목록에서 뺀다.
    """
    subjects = [s for s in (b2_payload or {}).get("subjects") or []
                if isinstance(s, dict) and s.get("classification") == "entity_state"]
    by_value = {}
    for vs in (e1_payload or {}).get("value_sites") or []:
        if not isinstance(vs, dict):
            continue
        expr = ((vs.get("value") or {}) if isinstance(vs.get("value"), dict) else {}
                ).get("code_expression")
        if isinstance(expr, str):
            by_value[expr] = vs

    out = []
    for vi, subject in enumerate(subjects):
        expr = _value_expression(subject)
        if not isinstance(expr, str) or not expr.strip():
            continue
        extra = by_value.get(expr) or {}
        declaration = ((extra.get("value") or {}).get("declaration")
                       if isinstance(extra.get("value"), dict) else None)
        if declaration is None:
            ident = subject.get("identity_evidence") or []
            declaration = ident[0] if ident else None

        entry = {"value": expr, "declaration": declaration, "writes": [], "reads": []}
        for field, seeds, added in (
                ("writes", subject.get("state_evidence"), extra.get("writes")),
                ("reads", subject.get("consumption_evidence"), extra.get("reads"))):
            letter = "w" if field == "writes" else "r"
            seen, bucket = set(), []
            for raws, origin in ((seeds, "b2"), (added, "e1")):
                for raw in raws or []:
                    s = _site(raw, f"e1:v{vi}:{letter}{len(bucket)}", origin)
                    if s is None or _key(s) in seen:
                        continue
                    seen.add(_key(s))
                    bucket.append(s)
            entry[field] = bucket

        dropped = {c.get("site_id") for c in extra.get("corrections") or []
                   if isinstance(c, dict)}
        if dropped:
            entry["writes"] = [s for s in entry["writes"] if s["site_id"] not in dropped]
            entry["reads"] = [s for s in entry["reads"] if s["site_id"] not in dropped]
        out.append(entry)
    return out
```

- [ ] **Step 4: 시험이 통과하는지 본다**

Run: `python -m unittest tests.test_flow -v`
Expected: PASS (8건)

- [ ] **Step 5: 커밋**

```bash
git add sesx/flow.py tests/test_flow.py
git commit -m "feat(ses): b2 씨앗과 e1 보충을 자리 목록으로 합친다"
```

---

### Task 5: `flow.py` — 결합 파생

자리 목록과 e2 의 주체 판정에서 방향 있는 결합을 만든다. 같은 주체 쌍은 `internal_update` 로, 주체 미해결은 보류로 간다.

**Files:**
- Modify: `sesx/flow.py` (`derive` 를 더한다)
- Test: `tests/test_flow.py` (같은 파일에 더한다)

**Interfaces:**
- Consumes: `flow.merge_sites` (Task 4)
- Produces:
  - `flow.derive(sites: list[dict], e2_payload: dict) -> dict`
  - 반환 모양 — 조립기가 그대로 먹을 수 있는 단계 payload 다:
    ```python
    {"couplings": [{"source": {"entity": str, "attribute": str|None, "symbol": None},
                    "target": {"entity": str, "attribute": str|None, "symbol": None},
                    "payload": {"kind": "shared_state", "code_expression": str,
                                "meaning": None},
                    "mechanism": str, "derived_from": [site_id, ...],
                    "evidence": [ev, ...]}],
     "internal_updates": [{"entity": str, "value": str,
                           "write_site_id": str, "read_site_id": str}],
     "unresolved_pairs": [{"value": str, "write_site_id": str, "read_site_id": str,
                           "why": str}]}
    ```

- [ ] **Step 1: 실패하는 시험을 쓴다**

`tests/test_flow.py` 끝(`if __name__` 위)에 더한다:

```python
SITES = [{"value": "fill[b][t]",
          "declaration": ev(254, "double[][] fill = new double[nB][nT];"),
          "writes": [{"site_id": "e1:v0:w0", "file_path": "E.java", "start_line": 454,
                      "end_line": 454, "quote": "fill[de.building][t] += add",
                      "effect": "increase", "from": "b2"},
                     {"site_id": "e1:v0:w1", "file_path": "E.java", "start_line": 424,
                      "end_line": 424, "quote": "fill[ce.building][t] *= keepFraction",
                      "effect": "decrease", "from": "e1"}],
          "reads": [{"site_id": "e1:v0:r0", "file_path": "E.java", "start_line": 414,
                     "end_line": 414, "quote": "dueTotal += fill[ce.building][t]",
                     "effect": "unknown", "from": "b2"},
                    {"site_id": "e1:v0:r1", "file_path": "E.java", "start_line": 470,
                     "end_line": 470, "quote": "worst = max(...)",
                     "effect": "unknown", "from": "e1"}]}]

E2 = {"site_actors": [
    {"site_id": "e1:v0:w0", "entity": "배출", "attribute": None},
    {"site_id": "e1:v0:w1", "entity": "수거", "attribute": None},
    {"site_id": "e1:v0:r0", "entity": "수거판정", "attribute": None},
    {"site_id": "e1:v0:r1", "entity": "넘침판정", "attribute": None}]}


class DeriveTests(unittest.TestCase):
    def pairs(self, result):
        return {(c["source"]["entity"], c["target"]["entity"])
                for c in result["couplings"]}

    def test_다른_주체_쌍만_결합이_된다(self):
        r = flow.derive(SITES, E2)
        self.assertEqual(self.pairs(r), {("배출", "수거판정"), ("배출", "넘침판정"),
                                         ("수거", "넘침판정")})

    def test_같은_주체_쌍은_상태_갱신으로_간다(self):
        r = flow.derive(SITES, E2)
        self.assertEqual(len(r["internal_updates"]), 1)
        self.assertEqual(r["internal_updates"][0]["entity"], "수거")

    def test_자기결합이_나오지_않는다(self):
        r = flow.derive(SITES, E2)
        for c in r["couplings"]:
            self.assertNotEqual(c["source"]["entity"], c["target"]["entity"])

    def test_payload_는_shared_state_로_확정된다(self):
        r = flow.derive(SITES, E2)
        self.assertEqual({c["payload"]["kind"] for c in r["couplings"]}, {"shared_state"})

    def test_같은_끝점_쌍은_결합_하나로_합쳐진다(self):
        sites = [dict(SITES[0], writes=SITES[0]["writes"] + [
            {"site_id": "e1:v0:w2", "file_path": "E.java", "start_line": 500,
             "end_line": 500, "quote": "fill[x][t] += 1", "effect": "increase",
             "from": "e1"}])]
        e2 = {"site_actors": E2["site_actors"] + [
            {"site_id": "e1:v0:w2", "entity": "배출", "attribute": None}]}
        r = flow.derive(sites, e2)
        merged = [c for c in r["couplings"]
                  if (c["source"]["entity"], c["target"]["entity"]) == ("배출", "수거판정")]
        self.assertEqual(len(merged), 1)
        self.assertIn("e1:v0:w2", merged[0]["derived_from"])

    def test_기여한_자리_인용이_전부_붙는다(self):
        r = flow.derive(SITES, E2)
        c = next(x for x in r["couplings"]
                 if (x["source"]["entity"], x["target"]["entity"]) == ("배출", "수거판정"))
        lines = sorted(e["start_line"] for e in c["evidence"])
        self.assertEqual(lines, [254, 414, 454])

    def test_주체_미해결이면_보류로_간다(self):
        e2 = {"site_actors": [{"site_id": "e1:v0:w0", "entity": None,
                               "why": "맞는 개체가 없다"},
                              {"site_id": "e1:v0:r0", "entity": "수거판정"}]}
        sites = [{"value": "fill[b][t]", "declaration": None,
                  "writes": [SITES[0]["writes"][0]], "reads": [SITES[0]["reads"][0]]}]
        r = flow.derive(sites, e2)
        self.assertEqual(r["couplings"], [])
        self.assertEqual(len(r["unresolved_pairs"]), 1)
        self.assertEqual(r["unresolved_pairs"][0]["write_site_id"], "e1:v0:w0")

    def test_주체_판정이_없는_자리도_보류다(self):
        sites = [{"value": "fill[b][t]", "declaration": None,
                  "writes": [SITES[0]["writes"][0]], "reads": [SITES[0]["reads"][0]]}]
        r = flow.derive(sites, {"site_actors": []})
        self.assertEqual(r["couplings"], [])
        self.assertEqual(len(r["unresolved_pairs"]), 1)

    def test_끝점_속성이_있으면_옮긴다(self):
        e2 = {"site_actors": [{"site_id": "e1:v0:w0", "entity": "배출", "attribute": "배출량"},
                              {"site_id": "e1:v0:r0", "entity": "수거판정",
                               "attribute": "판정"}]}
        sites = [{"value": "fill[b][t]", "declaration": None,
                  "writes": [SITES[0]["writes"][0]], "reads": [SITES[0]["reads"][0]]}]
        c = flow.derive(sites, e2)["couplings"][0]
        self.assertEqual(c["source"]["attribute"], "배출량")
        self.assertEqual(c["target"]["attribute"], "판정")
```

- [ ] **Step 2: 시험이 실패하는지 본다**

Run: `python -m unittest tests.test_flow -v`
Expected: FAIL — `AttributeError: module 'sesx.flow' has no attribute 'derive'`

- [ ] **Step 3: `derive` 를 쓴다**

`sesx/flow.py` 끝에 더한다:

```python
def _actors(e2_payload):
    out = {}
    for a in (e2_payload or {}).get("site_actors") or []:
        if not isinstance(a, dict) or not isinstance(a.get("site_id"), str):
            continue
        name = a.get("entity")
        if not isinstance(name, str) or not name.strip():
            continue        # unresolved. 주체를 못 정한 자리다
        out[a["site_id"]] = {"entity": name, "attribute": a.get("attribute")}
    return out


def _evidence(site):
    return {"file_path": site["file_path"], "start_line": site["start_line"],
            "end_line": site["end_line"], "quote": site["quote"]}


def derive(sites, e2_payload):
    """자리 목록과 주체 판정에서 방향 있는 결합을 만든다.

    값마다 `쓰는 자리 × 읽는 자리` 를 전부 짝짓는다. 주체가 다르면 결합, 같으면
    상태 갱신, 하나라도 미해결이면 보류다. 같은 (출발·속성, 도착·속성, 값) 은
    결합 하나로 합치고 기여한 자리를 전부 단다.
    """
    actors = _actors(e2_payload)
    merged, order = {}, []
    internal, pending = [], []

    for entry in sites or []:
        value = entry.get("value")
        declaration = entry.get("declaration")
        for w in entry.get("writes") or []:
            for r in entry.get("reads") or []:
                aw, ar = actors.get(w["site_id"]), actors.get(r["site_id"])
                if aw is None or ar is None:
                    missing = [s["site_id"] for s, a in ((w, aw), (r, ar)) if a is None]
                    pending.append({"value": value,
                                    "write_site_id": w["site_id"],
                                    "read_site_id": r["site_id"],
                                    "why": f"주체 미해결: {missing}"})
                    continue
                if aw["entity"] == ar["entity"]:
                    internal.append({"entity": aw["entity"], "value": value,
                                     "write_site_id": w["site_id"],
                                     "read_site_id": r["site_id"]})
                    continue
                key = (aw["entity"], aw["attribute"], ar["entity"], ar["attribute"], value)
                if key not in merged:
                    merged[key] = {
                        "source": {"entity": aw["entity"], "attribute": aw["attribute"],
                                   "symbol": None},
                        "target": {"entity": ar["entity"], "attribute": ar["attribute"],
                                   "symbol": None},
                        "payload": {"kind": "shared_state", "code_expression": value,
                                    "meaning": None},
                        "mechanism": None,
                        "derived_from": [],
                        "evidence": [declaration] if declaration else [],
                        "_lines": set(),
                    }
                    order.append(key)
                item = merged[key]
                for s in (w, r):
                    if s["site_id"] not in item["derived_from"]:
                        item["derived_from"].append(s["site_id"])
                    line = (s["file_path"], s["start_line"])
                    if line not in item["_lines"]:
                        item["_lines"].add(line)
                        item["evidence"].append(_evidence(s))

    couplings = []
    for key in order:
        item = merged[key]
        lines = sorted(str(ln) for _, ln in item.pop("_lines"))
        item["mechanism"] = (f"공유 상태 {key[4]} 를 통해 전달된다. "
                             f"기여한 행: {', '.join(lines)}")
        couplings.append(item)
    return {"couplings": couplings, "internal_updates": internal,
            "unresolved_pairs": pending}
```

- [ ] **Step 4: 시험이 통과하는지 본다**

Run: `python -m unittest tests.test_flow -v`
Expected: PASS (17건 — Task 4 의 8건 포함)

- [ ] **Step 5: 커밋**

```bash
git add sesx/flow.py tests/test_flow.py
git commit -m "feat(ses): 쓰는 자리와 읽는 자리를 짝지어 결합을 만든다"
```

---

### Task 6: 조립기가 파생 결과를 받는다

`flow.derive` 의 산출물을 단계 payload 로 먹인다. 결합은 기존 `_couplings` 가 그대로 처리하되 `derived_from` 을 옮기고, `internal_updates` 와 `unresolved_pairs` 는 각각 판정 기록과 보류로 간다 — 조용한 삭제가 없다.

**Files:**
- Modify: `sesx/assemble.py:141-159` (`build` 반복), `sesx/assemble.py:509-545` (`_couplings`)
- Test: `tests/test_flow_assembly.py`

**Interfaces:**
- Consumes: `flow.derive` 의 반환 모양 (Task 5)
- Produces: 단계 이름 `"flow"` 로 넣은 payload 가 조립된다. 결합 항목에 `"derived_from"` 키가 있다. `internal_updates` 는 `provenance` 에 `decision: "held"`, `unresolved_pairs` 는 `unresolved` 에 `reason_code: "unknown_reference"` 로 남는다.

- [ ] **Step 1: 실패하는 시험을 쓴다**

`tests/test_flow_assembly.py` 를 만든다:

```python
# -*- coding: utf-8 -*-
import unittest
from sesx import assemble, stages

EV = [{"file_path": "E.java", "start_line": 1, "end_line": 1, "quote": "q"}]


def entity(name, kind="stateful"):
    return {"name": name, "kind": kind, "scope": "simulation_target",
            "identity_evidence": EV, "composition_evidence": EV}


FLOW = {"couplings": [{
            "source": {"entity": "배출", "attribute": None, "symbol": None},
            "target": {"entity": "넘침판정", "attribute": None, "symbol": None},
            "payload": {"kind": "shared_state", "code_expression": "fill[b][t]",
                        "meaning": None},
            "mechanism": "공유 상태 fill[b][t] 를 통해 전달된다. 기여한 행: 454, 470",
            "derived_from": ["e1:v0:w0", "e1:v0:r1"],
            "evidence": EV}],
        "internal_updates": [{"entity": "수거", "value": "fill[b][t]",
                              "write_site_id": "e1:v0:w1",
                              "read_site_id": "e1:v0:r0"}],
        "unresolved_pairs": [{"value": "fill[b][t]", "write_site_id": "e1:v0:w2",
                              "read_site_id": "e1:v0:r0",
                              "why": "주체 미해결: ['e1:v0:w2']"}]}


def build():
    return assemble.build(
        {"g": {"entities": [entity("배출"), entity("넘침판정"), entity("수거")]},
         "flow": FLOW},
        "s", "a", {"strategy": "T2-flow"}, {},
        no_new_entity_stages=stages.NO_NEW_ENTITY_STAGES)


class FlowAssemblyTests(unittest.TestCase):
    def test_결합이_조립된다(self):
        doc = build()
        self.assertEqual(len(doc["couplings"]), 1)

    def test_파생_출처가_남는다(self):
        doc = build()
        self.assertEqual(doc["couplings"][0]["derived_from"],
                         ["e1:v0:w0", "e1:v0:r1"])

    def test_payload_가_확정이라_보류되지_않는다(self):
        doc = build()
        self.assertNotIn("payload_unknown",
                         [u["reason_code"] for u in doc["unresolved"]])

    def test_상태_갱신은_판정_기록으로_남는다(self):
        doc = build()
        rec = [p for p in doc["provenance"]
               if p.get("raw_id", "").startswith("flow:internal_update")]
        self.assertEqual(len(rec), 1)
        self.assertEqual(rec[0]["decision"], "held")
        self.assertTrue(rec[0]["origin_raw"])

    def test_주체_미해결_쌍은_보류로_남는다(self):
        doc = build()
        held = [u for u in doc["unresolved"]
                if u["raw_id"].startswith("flow:unresolved_pair")]
        self.assertEqual(len(held), 1)
        self.assertEqual(held[0]["reason_code"], "unknown_reference")
        self.assertTrue(held[0]["origin_raw"])

    def test_조립_결과가_계약을_통과한다(self):
        from sesx import contract
        doc = build()
        codes = [f["code"] for f in contract.validate_artifact(doc)]
        self.assertNotIn("SELF_COUPLING", codes)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 시험이 실패하는지 본다**

Run: `python -m unittest tests.test_flow_assembly -v`
Expected: FAIL — `derived_from` 이 결합 항목에 없고, `internal_updates`·`unresolved_pairs` 가 아무 데도 남지 않는다.

- [ ] **Step 3: 조립기를 고친다**

`_couplings` 의 `item` 사전에 `derived_from` 을 더한다 (`"mechanism": c.get("mechanism"),` 아래):

```python
                "derived_from": list(c.get("derived_from") or []),
```

`build` 의 결합 반복 뒤에 새 처리를 붙인다:

```python
        for stage, payload in stages.items():
            self._decompositions(stage, payload)
            self._couplings(stage, payload)
            self._flow_records(stage, payload)
```

그리고 `_couplings` 아래에 처리기를 더한다:

```python
    def _flow_records(self, stage, payload):
        """파생이 결합으로 내지 않은 쌍들. **버리지 않고 기록으로 옮긴다.**

        같은 주체끼리의 쌍은 결합이 아니라 상태 갱신이다. 주체를 못 정한 쌍은
        자리 인용이 살아 있으므로 사람이 주체만 붙이면 살아난다.
        """
        for i, u in enumerate(payload.get("internal_updates") or []):
            self._prov(stage, f"{stage}:internal_update:{i}", u, "held",
                       f"{u.get('entity')} 가 쓰고 같은 개체가 읽는다 — "
                       f"결합이 아니라 상태 갱신이다")
        for i, p in enumerate(payload.get("unresolved_pairs") or []):
            self._hold(stage, f"{stage}:unresolved_pair:{i}", p, "unknown_reference",
                       p.get("why") or "주체 미해결",
                       "자리 인용은 살아 있다. 주체를 붙이면 결합이 된다.")
            self._prov(stage, f"{stage}:unresolved_pair:{i}", p, "held", "주체 미해결")
```

- [ ] **Step 4: 시험이 통과하는지 본다**

Run: `python -m unittest tests.test_flow_assembly -v`
Expected: PASS (6건)

- [ ] **Step 5: 기존 시험을 돌린다**

Run: `python -m unittest discover -s tests`
Expected: OK

- [ ] **Step 6: 커밋**

```bash
git add sesx/assemble.py tests/test_flow_assembly.py
git commit -m "feat(ses): 조립기가 파생 결합과 그 잔여를 함께 받는다"
```

---

### Task 7: 프롬프트 — e1·e2 와 d.md 보강

모델에게 던지는 질문을 바꾼다. e1 은 자리만, e2 는 주체만 묻는다. d.md 에는 축 이름과 배열 차원 규칙을 더한다.

**Files:**
- Create: `sesx/prompts/T2/e1.md`, `sesx/prompts/T2/e2.md`
- Modify: `sesx/prompts/T2/d.md`
- Test: `tests/test_prompts_flow.py`

**Interfaces:**
- Consumes: 없음
- Produces: `stages.system_prompt("e1")` · `("e2")` 가 파일을 읽어 낸다 (배선은 Task 8)

- [ ] **Step 1: 실패하는 시험을 쓴다**

`tests/test_prompts_flow.py` 를 만든다:

```python
# -*- coding: utf-8 -*-
import os
import unittest

HERE = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
PROMPTS = os.path.join(HERE, "sesx", "prompts", "T2")


def read(name):
    with open(os.path.join(PROMPTS, name), encoding="utf-8") as f:
        return f.read()


class PromptTests(unittest.TestCase):
    def test_e1_이_있고_개체_이름을_금한다(self):
        t = read("e1.md")
        self.assertIn("개체 이름을 적지 않는다", t)
        self.assertIn("value_sites", t)
        self.assertIn("corrections", t)

    def test_e2_가_확정_개체만_고르게_한다(self):
        t = read("e2.md")
        self.assertIn("site_actors", t)
        self.assertIn("unresolved", t)
        self.assertIn("별개의 자리", t)

    def test_d_가_축_이름을_묻는다(self):
        self.assertIn("label", read("d.md"))

    def test_d_가_배열_차원_규칙을_적는다(self):
        self.assertIn("같은 선언", read("d.md"))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 시험이 실패하는지 본다**

Run: `python -m unittest tests.test_prompts_flow -v`
Expected: FAIL — `FileNotFoundError: e1.md`

- [ ] **Step 3: 프롬프트를 쓴다**

`sesx/prompts/T2/e1.md`:

```markdown
질문: 아래 상태 값마다, 그 값을 **쓰는 행**과 **읽는 행** 중 목록에 **빠진 것**을 인용하세요.

앞 단계가 이미 적은 자리는 씨앗으로 아래에 붙어 있습니다. **씨앗을 다시 적지 마세요.**
빠진 자리만 냅니다. 씨앗 중 그 값의 쓰기·읽기가 아닌 것이 있으면 `corrections` 에
자리 식별자와 이유를 적습니다.

**개체 이름을 적지 않습니다.** 이 단계는 자리만 봅니다. 누가 그 자리의 주체인지는 다음
단계의 질문입니다. 여기서 이름을 지어내면 다음 단계가 검증할 것이 없어집니다.

- **쓰는 행** — 그 값이 바뀌는 자리. 대입·증감·컬렉션 변형.
- **읽는 행** — 그 값이 읽혀 다음 계산·분기·이벤트를 바꾸는 자리. 결과를 담아 내보내는
  자리(`result.setX(...)`)는 읽기가 아닙니다 — 그것은 관측으로 나가는 길입니다.

`effect` 는 연산자에서 읽히는 것만 적습니다: `increase` · `decrease` · `assign`.
모르면 `unknown` 입니다.

```json
{"value_sites": [{"value": {"code_expression": "fill[b][t]", "declaration": {"file_path": "...", "start_line": 1, "end_line": 1, "quote": "..."}}, "writes": [{"file_path": "...", "start_line": 1, "end_line": 1, "quote": "...", "effect": "unknown"}], "reads": [{"file_path": "...", "start_line": 1, "end_line": 1, "quote": "..."}], "corrections": [{"site_id": "...", "why": "..."}]}]}
```

`value.code_expression` 은 아래 목록의 값 표현식을 **글자 그대로** 씁니다. 그래야 씨앗과
합쳐집니다.
```

`sesx/prompts/T2/e2.md`:

```markdown
질문: 아래 각 자리에서 **어느 개체를 대신해** 값이 바뀌거나 읽히는가.

자리마다 식별자와 인용이 붙어 있습니다. 자리 하나에 판정 하나를 냅니다.

이름은 **확정 개체 목록에서만** 고릅니다. 목록에 맞는 개체가 없으면 `entity` 를 `null` 로
두고 `why` 에 이유를 적습니다 — 그것이 정상적인 답입니다. 목록에 없는 이름을 지어내지
마세요.

**주체 근거는 자리 인용과 별개의 자리를 요구합니다.** `fill[ce.building][t] *= keepFraction`
한 줄로는 `ce` 가 무엇인지 알 수 없습니다. 그것을 말해 주는 것은 그 자리를 감싸는 선언이나
반복문 머리입니다 — 거기를 인용하세요. 자리 인용을 그대로 다시 적으면 근거가 아닙니다.

`attribute` 는 그 자리에서 갱신되거나 소비되는 **그 개체의 속성**입니다. 확정 속성 목록에
없거나 모르면 `null` 입니다.

```json
{"site_actors": [{"site_id": "e1:v0:w0", "entity": "개체 이름 또는 null", "attribute": null, "why": "...", "evidence": [{"file_path": "...", "start_line": 1, "end_line": 1, "quote": "..."}]}]}
```

`site_id` 는 아래 목록의 것을 글자 그대로 씁니다.
```

`sesx/prompts/T2/d.md` 에 두 문단을 더한다. `**한 개체는 부모를 하나만 가진다.**` 문단 **앞**에 넣는다:

```markdown
**분해에 축 이름을 답니다.** `label` 에 그 분해가 무엇으로 가르는지 적으세요 — `시스템 구성`,
`직업 축`, `거주민 다중` 처럼. 이름이 없으면 보류됩니다.

**같은 선언의 서로 다른 차원을 부모-자식으로 묶지 마세요.** `new double[nB][nT]` 의 두 축은
서로 **형제**이지 한쪽이 다른 쪽의 부분이 아닙니다. 두 자식의 역할 근거가 같은 선언 행을
가리키면 기각됩니다. 두 축을 다 남기려면 그 둘을 함께 갖는 상위 개체를 부모로 두고 ASPECT 로
가르세요.
```

`d.md` 끝의 JSON 예시에 `"label"` 을 더한다:

```json
{"decompositions": [{"parent": "...", "label": "축 이름", "kind": "ASPECT|SPEC|MULTI", "members": ["..."], "selection": {"symbol": "...", "description": "..."}, "multiplicity": {"count_expression": null, "minimum": null, "maximum": null, "evidence": []}, "member_evidence": {"자식이름": [{"file_path": "...", "start_line": 1, "end_line": 1, "quote": "..."}]}, "activation": {"state": "unknown", "clauses": [], "combination": null, "coverage": "incomplete"}, "evidence": [{"file_path": "...", "start_line": 1, "end_line": 1, "quote": "..."}]}]}
```

- [ ] **Step 4: 시험이 통과하는지 본다**

Run: `python -m unittest tests.test_prompts_flow -v`
Expected: PASS (4건)

- [ ] **Step 5: 커밋**

```bash
git add sesx/prompts/T2/e1.md sesx/prompts/T2/e2.md sesx/prompts/T2/d.md tests/test_prompts_flow.py
git commit -m "feat(ses): 자리와 주체를 따로 묻는 프롬프트를 쓴다"
```

---

### Task 8: 배선 — `T2-flow` 계획

새 계획을 단계 표에 올린다. `r`·`r2` 가 없고, `e` 대신 `e1`·`e2` 가 들어가며, 그 사이에 `flow.py` 가 낀다. 기존 계획은 손대지 않는다.

**Files:**
- Modify: `sesx/stages.py:24-57` (`PLANS`, `PROMPT_FILES`, `CONTEXT_FROM`, `NAME_LIST_STAGES`), `sesx/stages.py:84-120` (`_context_block`)
- Test: `tests/test_plan_flow.py`

**Interfaces:**
- Consumes: 프롬프트 파일 (Task 7), `flow.merge_sites`·`flow.derive` (Task 4·5)
- Produces:
  - `stages.PLANS["T2-flow"] == ("a", "b2", "g", "c", "d", "e1", "e2", "f", "b3")`
  - `stages.flow_payload(payloads: dict) -> dict` — `payloads` 의 `b2`·`e1`·`e2` 로 `flow.derive` 결과를 낸다. 조립기에 `"flow"` 단계로 넣는 값이다.

- [ ] **Step 1: 실패하는 시험을 쓴다**

`tests/test_plan_flow.py` 를 만든다:

```python
# -*- coding: utf-8 -*-
import unittest
from sesx import stages


class PlanTests(unittest.TestCase):
    def test_계획이_있다(self):
        self.assertEqual(stages.PLANS["T2-flow"],
                         ("a", "b2", "g", "c", "d", "e1", "e2", "f", "b3"))

    def test_루트_단계가_없다(self):
        self.assertNotIn("r", stages.PLANS["T2-flow"])
        self.assertNotIn("r2", stages.PLANS["T2-flow"])

    def test_기존_계획을_건드리지_않았다(self):
        self.assertEqual(stages.PLANS["T2-tree"],
                         ("a", "b2", "g", "c", "d", "r2", "e", "f", "b3"))

    def test_프롬프트가_이어진다(self):
        self.assertTrue(stages.system_prompt("e1").strip())
        self.assertTrue(stages.system_prompt("e2").strip())

    def test_e2_는_확정_이름_목록을_받는다(self):
        self.assertIn("e2", stages.NAME_LIST_STAGES)
        self.assertNotIn("e1", stages.NAME_LIST_STAGES)

    def test_e2_문맥에_자리_목록이_들어간다(self):
        b2 = {"subjects": [{"state": "fill[b][t]", "classification": "entity_state",
                            "owner_candidate": "수거지점",
                            "state_evidence": [{"file_path": "E.java", "start_line": 454,
                                                "end_line": 454, "quote": "w"}],
                            "identity_evidence": [], "linkage_evidence": [],
                            "consumption_evidence": [{"file_path": "E.java",
                                                      "start_line": 414, "end_line": 414,
                                                      "quote": "r"}]}]}
        block, _ = stages._context_block("e2", {"b2": b2, "e1": {"value_sites": []}}, 100000)
        self.assertIn("e1:v0:w0", block)
        self.assertIn("e1:v0:r0", block)


class FlowPayloadTests(unittest.TestCase):
    def test_payload_를_만든다(self):
        b2 = {"subjects": [{"state": "fill[b][t]", "classification": "entity_state",
                            "owner_candidate": "수거지점",
                            "state_evidence": [{"file_path": "E.java", "start_line": 454,
                                                "end_line": 454, "quote": "w"}],
                            "identity_evidence": [], "linkage_evidence": [],
                            "consumption_evidence": [{"file_path": "E.java",
                                                      "start_line": 414, "end_line": 414,
                                                      "quote": "r"}]}]}
        e2 = {"site_actors": [{"site_id": "e1:v0:w0", "entity": "배출"},
                              {"site_id": "e1:v0:r0", "entity": "수거판정"}]}
        out = stages.flow_payload({"b2": b2, "e1": {"value_sites": []}, "e2": e2})
        self.assertEqual(len(out["couplings"]), 1)
        self.assertEqual(out["couplings"][0]["source"]["entity"], "배출")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 시험이 실패하는지 본다**

Run: `python -m unittest tests.test_plan_flow -v`
Expected: FAIL — `KeyError: 'T2-flow'`

- [ ] **Step 3: `stages.py` 를 고친다**

`PLANS` 에 항목을 더한다 (기존 항목은 그대로 둔다):

```python
    # 색인 축이 아니라 값 흐름을 읽는 경로. 루트는 사람이 검토에서 넣는다.
    "T2-flow": ("a", "b2", "g", "c", "d", "e1", "e2", "f", "b3"),
```

`PROMPT_FILES` 에 더한다:

```python
    "e1": "T2/e1.md", "e2": "T2/e2.md",
```

`CONTEXT_FROM` 에 더한다:

```python
CONTEXT_FROM["e1"] = ("a", "b2")
CONTEXT_FROM["e2"] = ("b2", "e1", "g", "d")
```

`NAME_LIST_STAGES` 에 `"e2"` 를 더한다 (`"e1"` 은 이름을 쓰지 않으므로 넣지 않는다):

```python
NAME_LIST_STAGES = ("c", "d", "r", "r2", "e", "e2", "f")
```

`_context_block` 에 자리 목록 블록을 더한다. `if stage == "b3":` 옆에 같은 꼴로 넣는다:

```python
    if stage == "e1":
        vals = [s.get("state") for s in (payloads.get("b2") or {}).get("subjects") or []
                if isinstance(s, dict) and s.get("classification") == "entity_state"]
        block += ("\n## 자리를 채울 상태 값\n"
                  + "".join(f"  - {v}\n" for v in vals if v))
    if stage == "e2":
        lines = []
        for entry in flow.merge_sites(payloads.get("b2") or {}, payloads.get("e1") or {}):
            lines.append(f"  값 {entry['value']}")
            for s in entry["writes"] + entry["reads"]:
                lines.append(f"    {s['site_id']}  {s['file_path']}:{s['start_line']}"
                             f"  {s['quote']}")
        block += "\n## 주체를 정할 자리\n" + "".join(l + "\n" for l in lines)
```

`stages.py` 머리에 `from . import flow` 를 더하고, 파생 도우미를 파일 끝에 쓴다:

```python
def flow_payload(payloads):
    """b2·e1·e2 에서 파생된 결합 payload. 조립기에 "flow" 단계로 넣는다."""
    sites = flow.merge_sites(payloads.get("b2") or {}, payloads.get("e1") or {})
    return flow.derive(sites, payloads.get("e2") or {})
```

- [ ] **Step 4: 시험이 통과하는지 본다**

Run: `python -m unittest tests.test_plan_flow -v`
Expected: PASS (7건)

- [ ] **Step 5: `extract.py` 가 파생을 부르게 한다**

`extract.py` 에서 조립기를 부르는 자리를 찾는다: `grep -n "assemble.build" extract.py`

계획이 `T2-flow` 일 때 `payloads` 에 `"flow"` 를 더해 넘긴다:

```python
    if plan == "T2-flow":
        payloads = dict(payloads)
        payloads["flow"] = stages.flow_payload(payloads)
```

- [ ] **Step 6: 전체 시험을 돌린다**

Run: `python -m unittest discover -s tests`
Expected: OK

- [ ] **Step 7: 커밋**

```bash
git add sesx/stages.py extract.py tests/test_plan_flow.py
git commit -m "feat(ses): T2-flow 경로를 단계 표에 올린다"
```

---

### Task 9: 루트를 검토 리비전으로 옮긴다

추출은 숲으로 끝난다. 사람이 루트 개체와 그것을 잇는 ASPECT 분해를 판정으로 넣고, 그렇게 만들어진 항목에는 `origin: "human"` 이 박힌다. 기존 `set_root` 는 있는 개체를 가리킬 뿐 개체를 만들지 못한다 — 만드는 판정을 더한다.

**Files:**
- Modify: `sesx/review.py:20` (`DECISIONS`), `sesx/review.py:23-78` (`apply_decisions`)
- Test: `tests/test_root_designation.py`

**Interfaces:**
- Consumes: `contract.new_artifact` 모양
- Produces:
  - `review.DECISIONS` 에 `"root_designation"` 포함
  - 판정 모양: `{"decision": "root_designation", "root": {"name": str, "why": str, "evidence": [ev]}, "label": str, "members": [entity_id], "reason": str}`
  - 만들어진 개체·분해에 `"origin": "human"`

- [ ] **Step 1: 실패하는 시험을 쓴다**

`tests/test_root_designation.py` 를 만든다:

```python
# -*- coding: utf-8 -*-
import unittest
from sesx import contract, review

EV = [{"file_path": "설계문서.md", "start_line": 1, "end_line": 1,
       "quote": "장량동 생활쓰레기 수거 시뮬레이터"}]


def forest():
    """루트 없는 산출물. 상위 개체 둘이 서로 부모가 아니다."""
    doc = contract.new_artifact("a1")
    doc["entities"] = [
        {"id": "E1", "name": "생활쓰레기 수거 모델", "kind": "boundary",
         "scope": "simulation_target", "status": "proposed", "evidence_ids": []},
        {"id": "E2", "name": "시뮬레이션 결과", "kind": "boundary",
         "scope": "simulation_target", "status": "proposed", "evidence_ids": []},
    ]
    return doc


DECISION = {"decision": "root_designation",
            "root": {"name": "장량동 생활쓰레기 수거 시뮬레이터",
                     "why": "ref-v8 의 루트. 코드에 구문이 없어 사람이 넣는다",
                     "evidence": EV},
            "label": "시스템 구성",
            "members": ["E1", "E2"],
            "reason": "루트 지정"}


class RootDesignationTests(unittest.TestCase):
    def applied(self):
        return review.apply_decisions(forest(), [DECISION], "a2", reviewer="검토자")

    def test_루트_개체가_생긴다(self):
        out = self.applied()
        names = [e["name"] for e in out["entities"]]
        self.assertIn("장량동 생활쓰레기 수거 시뮬레이터", names)

    def test_루트에_사람_출처가_박힌다(self):
        out = self.applied()
        root = next(e for e in out["entities"]
                    if e["name"] == "장량동 생활쓰레기 수거 시뮬레이터")
        self.assertEqual(root["origin"], "human")
        self.assertEqual(root["kind"], "boundary")

    def test_분해가_생기고_축_이름이_붙는다(self):
        out = self.applied()
        self.assertEqual(len(out["decompositions"]), 1)
        d = out["decompositions"][0]
        self.assertEqual(d["label"], "시스템 구성")
        self.assertEqual(d["kind"], "ASPECT")
        self.assertEqual(d["origin"], "human")
        self.assertEqual([m["entity_id"] for m in d["members"]], ["E1", "E2"])

    def test_원본은_바뀌지_않는다(self):
        doc = forest()
        review.apply_decisions(doc, [DECISION], "a2")
        self.assertEqual(len(doc["entities"]), 2)
        self.assertEqual(doc["decompositions"], [])

    def test_없는_구성원을_가리키면_거부한다(self):
        bad = dict(DECISION, members=["E1", "E99"])
        with self.assertRaises(ValueError):
            review.apply_decisions(forest(), [bad], "a2")

    def test_근거_없는_루트를_거부한다(self):
        bad = dict(DECISION, root={"name": "시스템", "why": "", "evidence": []})
        with self.assertRaises(ValueError):
            review.apply_decisions(forest(), [bad], "a2")

    def test_판정이_기록에_남는다(self):
        out = self.applied()
        kinds = [x["decision"] for x in out["review_status"]["log"]]
        self.assertIn("root_designation", kinds)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 시험이 실패하는지 본다**

Run: `python -m unittest tests.test_root_designation -v`
Expected: FAIL — `ValueError: 알 수 없는 판정: 'root_designation'`

- [ ] **Step 3: `review.py` 를 고친다**

`DECISIONS` 에 더한다:

```python
DECISIONS = ("accept", "reject", "hold", "set_root", "root_designation", "note")
```

`apply_decisions` 의 `if kind == "set_root":` **앞**에 처리를 넣는다:

```python
        if kind == "root_designation":
            root = d.get("root") or {}
            name = (root.get("name") or "").strip()
            if not name or not (root.get("evidence") or []):
                raise ValueError("루트 지정에는 이름과 근거가 필요하다")
            members = list(d.get("members") or [])
            missing = [m for m in members if m not in index]
            if missing:
                raise ValueError(f"루트 구성원이 없다: {missing}")
            eid = f"E-human-{len(out['entities']) + 1}"
            evidence_ids = []
            for i, ev in enumerate(root.get("evidence") or []):
                ev_id = f"V-human-{eid}-{i}"
                out["evidence"].append(dict(ev, id=ev_id, item_id=eid,
                                            field="existence", origin="human",
                                            checks={"file": "not_checked",
                                                    "range": "not_checked",
                                                    "quote": "not_checked",
                                                    "symbol": "not_checked"}))
                evidence_ids.append(ev_id)
            out["entities"].append({
                "id": eid, "name": name, "kind": "boundary",
                "scope": "simulation_target", "why_entity": root.get("why"),
                "origin": "human", "status": "proposed", "evidence_ids": evidence_ids})
            did = f"D-human-{len(out['decompositions']) + 1}"
            aid = f"A-human-{len(out['activation']) + 1}"
            out["activation"].append({
                "id": aid, "state": "unknown", "clauses": [], "combination": None,
                "coverage": "incomplete", "origin": "human", "status": "proposed",
                "evidence_ids": []})
            out["decompositions"].append({
                "id": did, "parent_entity_id": eid, "kind": "ASPECT",
                "label": d.get("label"),
                "members": [{"entity_id": m} for m in members],
                "selection": None, "multiplicity": None,
                "activation_id": aid, "origin": "human",
                "status": "proposed", "evidence_ids": evidence_ids})
            out.setdefault("structure", {})["reviewer_root"] = {
                "entity_id": eid, "reason": d.get("reason"),
                "reviewer": reviewer, "at": time.time()}
            out["provenance"].append({
                "id": f"P-review-root-{len(out['provenance']) + 1}",
                "origin_stage": "review", "raw_id": None, "origin_raw": d,
                "decision": "kept",
                "reason": "사람이 루트를 지정했다 — 코드에 근거가 없다",
                "input_item_ids": members, "output_item_ids": [eid, did]})
            log.append({"decision": kind, "item_id": eid, "reason": d.get("reason")})
            continue
```

`evidence` 항목의 키 이름은 `sesx/evidence.py` 가 쓰는 것과 맞춰야 한다. 확인: `grep -n "\"checks\"\|def record\|field" sesx/evidence.py | head`. 어긋나면 그 모양을 따른다.

- [ ] **Step 4: 시험이 통과하는지 본다**

Run: `python -m unittest tests.test_root_designation -v`
Expected: PASS (7건)

- [ ] **Step 5: 재검증이 도는지 본다**

```bash
python -c "
from sesx import contract, review
import tests.test_root_designation as t
out = review.apply_decisions(t.forest(), [t.DECISION], 'a2', reviewer='x')
print([f['code'] for f in contract.validate_artifact(out)])
"
```
Expected: 빈 목록이거나, 나오는 위반 코드가 루트 지정과 무관한 것이어야 한다. 참조 위반(`ENTITY_REF_BROKEN` 등)이 나오면 만들어 넣은 항목의 키가 계약과 어긋난 것이므로 고친다.

- [ ] **Step 6: 커밋**

```bash
git add sesx/review.py tests/test_root_designation.py
git commit -m "feat(ses): 사람이 넣은 루트를 검토 리비전으로 받는다"
```

---

### Task 10: 보고서가 사람이 넣은 것을 가른다

`origin: "human"` 이 박힌 항목을 보고서에서 따로 센다. "파이프라인이 루트를 찾았다"는 주장이 생길 자리를 없앤다.

**Files:**
- Modify: `sesx/report.py`
- Test: `tests/test_report_origin.py`

**Interfaces:**
- Consumes: `origin: "human"` 이 박힌 항목 (Task 9)
- Produces: 보고서 본문에 `사람이 넣은 것` 절이 있고, 개체·분해 수가 `추출 N · 사람 M` 으로 갈린다

- [ ] **Step 1: 보고서가 지금 무엇을 내는지 본다**

Run: `grep -n "def \|entities\|decompositions" sesx/report.py | head -30`

보고서를 만드는 함수 이름과 반환형(문자열인지 사전인지)을 확인한다. 아래 시험의 `report.render(doc)` 를 **실제 이름으로 바꿔 쓴다.**

- [ ] **Step 2: 실패하는 시험을 쓴다**

`tests/test_report_origin.py` 를 만든다. `render` 는 Step 1 에서 확인한 이름으로 바꾼다:

```python
# -*- coding: utf-8 -*-
import unittest
from sesx import contract, report


def doc_with_human():
    doc = contract.new_artifact("a1")
    doc["entities"] = [
        {"id": "E1", "name": "수거지점", "kind": "stateful",
         "scope": "simulation_target", "status": "proposed", "evidence_ids": []},
        {"id": "E-human-1", "name": "장량동 생활쓰레기 수거 시뮬레이터",
         "kind": "boundary", "scope": "simulation_target", "origin": "human",
         "status": "proposed", "evidence_ids": []},
    ]
    doc["decompositions"] = [
        {"id": "D-human-1", "parent_entity_id": "E-human-1", "kind": "ASPECT",
         "label": "시스템 구성", "members": [{"entity_id": "E1"}],
         "activation_id": None, "origin": "human", "status": "proposed",
         "evidence_ids": []}]
    return doc


class OriginTests(unittest.TestCase):
    def test_사람이_넣은_것을_따로_센다(self):
        text = report.render(doc_with_human())
        self.assertIn("사람이 넣은 것", text)
        self.assertIn("장량동 생활쓰레기 수거 시뮬레이터", text)

    def test_추출과_사람_수가_갈린다(self):
        text = report.render(doc_with_human())
        self.assertIn("추출 1", text)
        self.assertIn("사람 1", text)

    def test_사람이_넣은_것이_없으면_절이_없다(self):
        doc = doc_with_human()
        doc["entities"] = [doc["entities"][0]]
        doc["decompositions"] = []
        self.assertNotIn("사람이 넣은 것", report.render(doc))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 3: 시험이 실패하는지 본다**

Run: `python -m unittest tests.test_report_origin -v`
Expected: FAIL — 보고서에 그 절이 없다

- [ ] **Step 4: `report.py` 에 절을 더한다**

보고서 본문을 만드는 함수 안, 개체·분해 수를 적는 자리 옆에 더한다:

```python
def _human_section(doc):
    """사람이 넣은 항목. 모델이 뽑은 것과 섞이지 않게 따로 센다."""
    rows = []
    for arr, 이름 in (("entities", "개체"), ("decompositions", "분해")):
        items = doc.get(arr) or []
        human = [it for it in items if it.get("origin") == "human"]
        if human:
            rows.append(f"- {이름}: 추출 {len(items) - len(human)} · 사람 {len(human)}")
            for it in human:
                rows.append(f"  - {it.get('name') or it.get('label') or it.get('id')}"
                            f" ({it['id']})")
    if not rows:
        return ""
    return ("\n## 사람이 넣은 것\n\n"
            "코드에 근거가 없어 검토에서 넣은 항목이다. 파이프라인이 뽑은 것이 아니다.\n\n"
            + "\n".join(rows) + "\n")
```

그리고 본문 조립부에 `+ _human_section(doc)` 를 붙인다.

- [ ] **Step 5: 시험이 통과하는지 본다**

Run: `python -m unittest tests.test_report_origin -v`
Expected: PASS (3건)

- [ ] **Step 6: 전체 시험을 돌린다**

Run: `python -m unittest discover -s tests`
Expected: OK

- [ ] **Step 7: 커밋**

```bash
git add sesx/report.py tests/test_report_origin.py
git commit -m "feat(ses): 보고서가 사람이 넣은 것을 따로 센다"
```

---

### Task 11: `score_ses.py` 가 결합을 잰다

ref-v8 의 결합 목록을 상대로 파생 결과를 잰다. 지금은 옛 계약 기준이라 결합을 재지 못한다.

**Files:**
- Modify: `score_ses.py`
- Test: `tests/test_score_couplings.py`

**Interfaces:**
- Consumes: `reference-ses.json` 의 `couplings` (`{"from": "개체.속성", "to": "개체.속성", "mechanism": ...}`), 조립된 산출물의 `couplings`
- Produces: `score_ses.score_couplings(doc, reference) -> {"matched": int, "missed": [str], "extra": [str], "precision": float, "recall": float}`

- [ ] **Step 1: 정답지의 결합 모양을 확인한다**

```bash
python -c "
import json
d = json.load(open('reference-ses.json', encoding='utf-8'))
print(json.dumps(d['couplings'][:2], ensure_ascii=False, indent=1))
print(len(d['couplings']))
"
```

`from`·`to` 가 `개체.속성` 문자열임을 눈으로 확인한다. 아래 시험은 이 모양을 전제한다.

- [ ] **Step 2: 실패하는 시험을 쓴다**

`tests/test_score_couplings.py` 를 만든다:

```python
# -*- coding: utf-8 -*-
import os
import sys
import unittest

sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import score_ses  # noqa: E402

REFERENCE = {"couplings": [
    {"from": "거주민.배출", "to": "수거지점.적재"},
    {"from": "수거지점.적재량", "to": "적재초과 판정.판정"}]}


def doc(pairs):
    entities, by_name = [], {}
    for name in {n.split(".")[0] for p in pairs for n in p}:
        eid = f"E{len(entities) + 1}"
        by_name[name] = eid
        entities.append({"id": eid, "name": name})
    attrs, couplings = [], []
    for i, (src, tgt) in enumerate(pairs):
        box = []
        for side in (src, tgt):
            ename, _, aname = side.partition(".")
            aid = None
            if aname:
                aid = f"T{len(attrs) + 1}"
                attrs.append({"id": aid, "entity_id": by_name[ename], "name": aname})
            box.append({"entity_id": by_name[ename], "attribute_id": aid})
        couplings.append({"id": f"C{i}", "source": box[0], "target": box[1],
                          "status": "proposed"})
    return {"entities": entities, "attributes": attrs, "couplings": couplings}


class ScoreTests(unittest.TestCase):
    def test_정확히_맞으면_둘_다_1이다(self):
        r = score_ses.score_couplings(
            doc([("거주민.배출", "수거지점.적재"),
                 ("수거지점.적재량", "적재초과 판정.판정")]), REFERENCE)
        self.assertEqual(r["matched"], 2)
        self.assertEqual(r["precision"], 1.0)
        self.assertEqual(r["recall"], 1.0)

    def test_빠뜨린_것을_센다(self):
        r = score_ses.score_couplings(doc([("거주민.배출", "수거지점.적재")]), REFERENCE)
        self.assertEqual(r["matched"], 1)
        self.assertEqual(r["missed"], ["수거지점.적재량 → 적재초과 판정.판정"])
        self.assertEqual(r["recall"], 0.5)

    def test_없는_것을_낸_것도_센다(self):
        r = score_ses.score_couplings(
            doc([("거주민.배출", "수거지점.적재"), ("트럭.적재", "트럭.적재")]), REFERENCE)
        self.assertEqual(r["extra"], ["트럭.적재 → 트럭.적재"])
        self.assertEqual(r["precision"], 0.5)

    def test_결합이_없으면_0이다(self):
        r = score_ses.score_couplings(doc([]), REFERENCE)
        self.assertEqual(r["matched"], 0)
        self.assertEqual(r["precision"], 0.0)
        self.assertEqual(r["recall"], 0.0)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 3: 시험이 실패하는지 본다**

Run: `python -m unittest tests.test_score_couplings -v`
Expected: FAIL — `AttributeError: module 'score_ses' has no attribute 'score_couplings'`

- [ ] **Step 4: `score_couplings` 를 쓴다**

`score_ses.py` 에 더한다:

```python
def _endpoint(doc, box):
    """{"entity_id": ..., "attribute_id": ...} -> "개체.속성" (속성이 없으면 "개체")."""
    ent = next((e for e in doc.get("entities") or []
                if e["id"] == (box or {}).get("entity_id")), None)
    if ent is None:
        return None
    name = ent.get("name")
    attr = next((a for a in doc.get("attributes") or []
                 if a["id"] == (box or {}).get("attribute_id")), None)
    return f"{name}.{attr['name']}" if attr else name


def score_couplings(doc, reference):
    """정답지 결합 대비 산출 결합. 끝점 이름으로 맞춘다.

    속성까지 적힌 정답지 끝점은 속성까지 같아야 맞는 것으로 센다. 산출 쪽 속성이
    비어 있으면 맞지 않은 것이다 — 모르는 것을 맞았다고 세지 않는다.
    """
    got = set()
    for c in doc.get("couplings") or []:
        if c.get("status") == "rejected":
            continue
        s, t = _endpoint(doc, c.get("source")), _endpoint(doc, c.get("target"))
        if s and t:
            got.add(f"{s} → {t}")
    want = {f"{c['from']} → {c['to']}" for c in reference.get("couplings") or []}
    matched = got & want
    return {"matched": len(matched),
            "missed": sorted(want - got),
            "extra": sorted(got - want),
            "precision": (len(matched) / len(got)) if got else 0.0,
            "recall": (len(matched) / len(want)) if want else 0.0}
```

- [ ] **Step 5: 시험이 통과하는지 본다**

Run: `python -m unittest tests.test_score_couplings -v`
Expected: PASS (4건)

- [ ] **Step 6: 전체 시험을 돌린다**

Run: `python -m unittest discover -s tests`
Expected: OK — 전부 통과

- [ ] **Step 7: 커밋**

```bash
git add score_ses.py tests/test_score_couplings.py
git commit -m "feat(ses): 정답지 결합 대비 채점을 더한다"
```

---

### Task 12: README 를 사실에 맞춘다

`sesx/README.md` 가 새 경로와 모듈을 적게 한다. 시험 건수도 실제와 맞춘다 — 지금 "47건"이라 적혀 있으나 실제로는 그보다 많다.

**Files:**
- Modify: `sesx/README.md`

**Interfaces:**
- Consumes: Task 1–11 의 결과
- Produces: 없음 (문서)

- [ ] **Step 1: 실제 시험 건수를 센다**

```bash
python -m unittest discover -s tests 2>&1 | tail -3
```

출력의 `Ran N tests` 에서 N 을 받아 적는다.

- [ ] **Step 2: 모듈 표에 두 줄을 더한다**

```markdown
| `shape.py` | 트리 모양 계약. ref-v8 의 세 층만 합법으로 둔다 |
| `flow.py` | 쓰는 자리와 읽는 자리를 짝지어 방향 있는 결합을 만든다. 모델이 참여하지 않는다 |
```

- [ ] **Step 3: 쓰는 법에 새 경로를 더한다**

```bash
python extract.py pipeline --rule P-pilot --run-id r1 --plan T2-flow --model gpt-4.1-mini
```

- [ ] **Step 4: 규칙 목록에 두 줄을 더한다**

```markdown
9. **결합의 방향은 코드가 만든다.** 모델은 자리(e1)와 주체(e2)만 답한다. 짝짓기·방향·
   자기쌍 제거는 `flow.py` 가 한다. 출발과 도착이 같은 결합은 계약 위반이다.
10. **배열 차원은 형제다.** 같은 선언의 서로 다른 차원을 부모-자식으로 묶은 분해는
    보류된다. 두 축을 다 남기려면 상위 개체를 두고 ASPECT 로 가른다.
```

- [ ] **Step 5: "아직 하지 않은 것" 에서 끝난 항목을 지운다**

`참조 대비 채점의 결합 … 확장` 줄을 지운다 (Task 11 에서 했다). `실제 모델로 돌린 S2·T2 비교`는 **남긴다** — 이 계획은 파일럿을 돌리지 않는다.

- [ ] **Step 6: 시험 건수를 고치고 전체 시험을 돌린다**

Run: `python -m unittest discover -s tests`
Expected: OK, `Ran N tests` 가 README 에 적은 수와 같다

- [ ] **Step 7: 커밋**

```bash
git add sesx/README.md
git commit -m "docs(ses): README 를 새 경로와 실제 시험 건수에 맞춘다"
```

---

## 이 계획이 하지 않는 것

- **파일럿을 돌리지 않는다.** 실제 모델로 `T2-flow` 를 돌려 S2·T2 와 비교하는 것은 별도 작업이다. 이 계획은 그 실행이 설 수 있는 바닥만 놓는다.
- **기존 산출물을 옮기지 않는다.** `schema_version` 이 `1.1` 로 오르지만 옛 산출물을 변환하는 경로를 만들지 않는다. 옛 것은 옛 판으로 읽힌다.
- **루트를 자동으로 만들지 않는다.** Task 9 는 사람이 넣을 **자리**를 만들 뿐이다. 장량동 모델의 루트를 실제로 넣는 것은 검토자의 판단이다.
- **`엔진 실행` 가지를 넣지 않는다.** 손그림 트리에 있으나 ref-v8 이 "실행 수단"으로 제외했고, 설계는 ref-v8 을 따른다.
