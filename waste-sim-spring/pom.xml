<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.0</version>
        <relativePath/>
    </parent>

    <groupId>com.wastesim</groupId>
    <artifactId>waste-sim-spring</artifactId>
    <!-- 애플리케이션 버전. 명세서 버전(v1.11)과는 별개 축이라 통일하지 않는다 —
         문서만 고치는 개정과 코드만 고치는 핫픽스가 따로 일어나기 때문이다.
         자세한 근거는 docs/specifications/README.md "버전 체계".
         실행 중인 서버가 어느 명세로 빌드됐는지는 /actuator/info가 둘을 함께 알려준다
         (application.properties의 info.spec.version). -->
    <version>1.1.1</version>
    <packaging>jar</packaging>
    <name>waste-sim-spring</name>
    <description>Community Household-Waste DEVS Simulation — Spring Boot + Chat UI</description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <!-- Spring Web (REST API) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring WebSocket (STOMP) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>

        <!-- Actuator + Micrometer (관측성: /actuator/health·metrics) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Jackson JSON -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- OkHttp — OpenAI 호환 LLM 백엔드(OpenAI·Ollama·Gemini) 호출용.
             프로바이더는 Spring 프로파일로 고른다(application-openai/ollama.properties).
             전용 SDK를 쓰지 않고 HTTP로 직접 부르는 이유가 그 호환성이다 — 백엔드를
             바꿔도 코드가 아니라 프로파일만 바뀐다(SDD 2.17.4). -->
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>okhttp</artifactId>
        </dependency>

        <!-- Lombok (optional boilerplate reduction) -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- MockWebServer — LLM 백엔드 응답을 고정 주입해 "LLM이 무엇을 돌려주든 서버의
             판단은 그대로"임을 검증한다(TDD 3.17.5, UT-328~332). 실제 모델을 띄우면
             그 검증이 모델의 그날 컨디션에 좌우된다 — 여기서 보는 것은 모델의 품질이
             아니라 <b>경계</b>다. 버전은 okhttp와 함께 스프링 부트 부모 POM이 관리한다. -->
        <dependency>
            <groupId>com.squareup.okhttp3</groupId>
            <artifactId>mockwebserver</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
